/*
 * SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
 * SPDX-License-Identifier: Apache-2.0
 */

// @generated by cuda-intrinsics-gen 0.1.0. DO NOT EDIT.
// Catalog schema/version: 44/0.1.0; intrinsic ABI: v1; catalog SHA-256: 1f72d053300ad70672b9cf851743dd05e0904f7222828eecd53d5ca88a4a0088.
// LLVM source: 1cb4e3833c1919c2e6fb579a23ac0e2b22587b7e.

//! Structural operations for the generated `shfl.sync` family.

use pliron::{
    builtin::{
        op_interfaces::{NOpdsInterface, NResultsInterface},
        types::{FP32Type, IntegerType, Signedness},
    },
    common_traits::Verify,
    context::{Context, Ptr},
    location::Located,
    op::Op,
    operation::Operation,
    result::Error,
    r#type::Typed,
    value::Value,
    verify_err,
};
use pliron_derive::pliron_op;

fn is_i32(ctx: &Context, ty: pliron::r#type::TypeHandle) -> bool {
    ty.deref(ctx)
        .downcast_ref::<IntegerType>()
        .is_some_and(|integer| integer.width() == 32)
}

fn is_i64(ctx: &Context, ty: pliron::r#type::TypeHandle) -> bool {
    ty.deref(ctx)
        .downcast_ref::<IntegerType>()
        .is_some_and(|integer| integer.width() == 64)
}

fn is_f32(ctx: &Context, ty: pliron::r#type::TypeHandle) -> bool {
    ty.deref(ctx).downcast_ref::<FP32Type>().is_some()
}

/// Read an f32 value from a higher named warp lane.
///
/// Operands are `[member_mask, value, lane_or_delta]`.
/// Generated lowering inserts the fixed clamp required by the selected shuffle mode.
#[pliron_op(
    name = "nvvm.shfl_sync_down_f32",
    format,
    interfaces = [NOpdsInterface<3>, NResultsInterface<1>],
)]
pub struct ShflSyncDownF32Op;

impl ShflSyncDownF32Op {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }

    pub fn build(
        ctx: &mut Context,
        member_mask: Value,
        value: Value,
        lane_or_delta: Value,
    ) -> Ptr<Operation> {
        let result_ty: pliron::r#type::TypeHandle = FP32Type::get(ctx).into();
        Operation::new(
            ctx,
            Self::get_concrete_op_info(),
            vec![result_ty],
            vec![member_mask, value, lane_or_delta],
            vec![],
            0,
        )
    }
}

impl Verify for ShflSyncDownF32Op {
    fn verify(&self, ctx: &Context) -> Result<(), Error> {
        let op = self.get_operation().deref(ctx);
        if op.get_num_operands() != 3 || op.get_num_results() != 1 {
            return verify_err!(
                op.loc(),
                "nvvm.shfl_sync_down_f32 requires exactly [member_mask, value, lane_or_delta] and one result"
            );
        }
        if !is_i32(ctx, op.get_operand(0).get_type(ctx))
            || !is_f32(ctx, op.get_operand(1).get_type(ctx))
            || !is_i32(ctx, op.get_operand(2).get_type(ctx))
            || !is_f32(ctx, op.get_result(0).get_type(ctx))
        {
            return verify_err!(
                op.loc(),
                "nvvm.shfl_sync_down_f32 requires i32 mask/lane and f32 value/result"
            );
        }
        Ok(())
    }
}

/// Read a 32-bit value from a higher named warp lane.
///
/// Operands are `[member_mask, value, lane_or_delta]`.
/// Generated lowering inserts the fixed clamp required by the selected shuffle mode.
#[pliron_op(
    name = "nvvm.shfl_sync_down_i32",
    format,
    interfaces = [NOpdsInterface<3>, NResultsInterface<1>],
)]
pub struct ShflSyncDownI32Op;

impl ShflSyncDownI32Op {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }

    pub fn build(
        ctx: &mut Context,
        member_mask: Value,
        value: Value,
        lane_or_delta: Value,
    ) -> Ptr<Operation> {
        let result_ty: pliron::r#type::TypeHandle =
            IntegerType::get(ctx, 32, Signedness::Unsigned).into();
        Operation::new(
            ctx,
            Self::get_concrete_op_info(),
            vec![result_ty],
            vec![member_mask, value, lane_or_delta],
            vec![],
            0,
        )
    }
}

impl Verify for ShflSyncDownI32Op {
    fn verify(&self, ctx: &Context) -> Result<(), Error> {
        let op = self.get_operation().deref(ctx);
        if op.get_num_operands() != 3 || op.get_num_results() != 1 {
            return verify_err!(
                op.loc(),
                "nvvm.shfl_sync_down_i32 requires exactly [member_mask, value, lane_or_delta] and one result"
            );
        }
        if !is_i32(ctx, op.get_operand(0).get_type(ctx))
            || !is_i32(ctx, op.get_operand(1).get_type(ctx))
            || !is_i32(ctx, op.get_operand(2).get_type(ctx))
            || !is_i32(ctx, op.get_result(0).get_type(ctx))
        {
            return verify_err!(
                op.loc(),
                "nvvm.shfl_sync_down_i32 requires i32 mask/lane and i32 value/result"
            );
        }
        Ok(())
    }
}

/// Read a 64-bit value from a higher named lane using two 32-bit shuffles.
///
/// Operands are `[member_mask, value, lane_or_delta]`.
/// Lowering splits the value into two `b32` halves, shuffles both, and reassembles it.
#[pliron_op(
    name = "nvvm.shfl_sync_down_i64",
    format,
    interfaces = [NOpdsInterface<3>, NResultsInterface<1>],
)]
pub struct ShflSyncDownI64Op;

impl ShflSyncDownI64Op {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }

    pub fn build(
        ctx: &mut Context,
        member_mask: Value,
        value: Value,
        lane_or_delta: Value,
    ) -> Ptr<Operation> {
        let result_ty: pliron::r#type::TypeHandle =
            IntegerType::get(ctx, 64, Signedness::Unsigned).into();
        Operation::new(
            ctx,
            Self::get_concrete_op_info(),
            vec![result_ty],
            vec![member_mask, value, lane_or_delta],
            vec![],
            0,
        )
    }
}

impl Verify for ShflSyncDownI64Op {
    fn verify(&self, ctx: &Context) -> Result<(), Error> {
        let op = self.get_operation().deref(ctx);
        if op.get_num_operands() != 3 || op.get_num_results() != 1 {
            return verify_err!(
                op.loc(),
                "nvvm.shfl_sync_down_i64 requires exactly [member_mask, value, lane_or_delta] and one result"
            );
        }
        if !is_i32(ctx, op.get_operand(0).get_type(ctx))
            || !is_i64(ctx, op.get_operand(1).get_type(ctx))
            || !is_i32(ctx, op.get_operand(2).get_type(ctx))
            || !is_i64(ctx, op.get_result(0).get_type(ctx))
        {
            return verify_err!(
                op.loc(),
                "nvvm.shfl_sync_down_i64 requires i32 mask/lane and i64 value/result"
            );
        }
        Ok(())
    }
}

/// Read an f32 value from a named warp lane.
///
/// Operands are `[member_mask, value, lane_or_delta]`.
/// Generated lowering inserts the fixed clamp required by the selected shuffle mode.
#[pliron_op(
    name = "nvvm.shfl_sync_idx_f32",
    format,
    interfaces = [NOpdsInterface<3>, NResultsInterface<1>],
)]
pub struct ShflSyncIdxF32Op;

impl ShflSyncIdxF32Op {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }

    pub fn build(
        ctx: &mut Context,
        member_mask: Value,
        value: Value,
        lane_or_delta: Value,
    ) -> Ptr<Operation> {
        let result_ty: pliron::r#type::TypeHandle = FP32Type::get(ctx).into();
        Operation::new(
            ctx,
            Self::get_concrete_op_info(),
            vec![result_ty],
            vec![member_mask, value, lane_or_delta],
            vec![],
            0,
        )
    }
}

impl Verify for ShflSyncIdxF32Op {
    fn verify(&self, ctx: &Context) -> Result<(), Error> {
        let op = self.get_operation().deref(ctx);
        if op.get_num_operands() != 3 || op.get_num_results() != 1 {
            return verify_err!(
                op.loc(),
                "nvvm.shfl_sync_idx_f32 requires exactly [member_mask, value, lane_or_delta] and one result"
            );
        }
        if !is_i32(ctx, op.get_operand(0).get_type(ctx))
            || !is_f32(ctx, op.get_operand(1).get_type(ctx))
            || !is_i32(ctx, op.get_operand(2).get_type(ctx))
            || !is_f32(ctx, op.get_result(0).get_type(ctx))
        {
            return verify_err!(
                op.loc(),
                "nvvm.shfl_sync_idx_f32 requires i32 mask/lane and f32 value/result"
            );
        }
        Ok(())
    }
}

/// Read a 32-bit value from a named warp lane.
///
/// Operands are `[member_mask, value, lane_or_delta]`.
/// Generated lowering inserts the fixed clamp required by the selected shuffle mode.
#[pliron_op(
    name = "nvvm.shfl_sync_idx_i32",
    format,
    interfaces = [NOpdsInterface<3>, NResultsInterface<1>],
)]
pub struct ShflSyncIdxI32Op;

impl ShflSyncIdxI32Op {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }

    pub fn build(
        ctx: &mut Context,
        member_mask: Value,
        value: Value,
        lane_or_delta: Value,
    ) -> Ptr<Operation> {
        let result_ty: pliron::r#type::TypeHandle =
            IntegerType::get(ctx, 32, Signedness::Unsigned).into();
        Operation::new(
            ctx,
            Self::get_concrete_op_info(),
            vec![result_ty],
            vec![member_mask, value, lane_or_delta],
            vec![],
            0,
        )
    }
}

impl Verify for ShflSyncIdxI32Op {
    fn verify(&self, ctx: &Context) -> Result<(), Error> {
        let op = self.get_operation().deref(ctx);
        if op.get_num_operands() != 3 || op.get_num_results() != 1 {
            return verify_err!(
                op.loc(),
                "nvvm.shfl_sync_idx_i32 requires exactly [member_mask, value, lane_or_delta] and one result"
            );
        }
        if !is_i32(ctx, op.get_operand(0).get_type(ctx))
            || !is_i32(ctx, op.get_operand(1).get_type(ctx))
            || !is_i32(ctx, op.get_operand(2).get_type(ctx))
            || !is_i32(ctx, op.get_result(0).get_type(ctx))
        {
            return verify_err!(
                op.loc(),
                "nvvm.shfl_sync_idx_i32 requires i32 mask/lane and i32 value/result"
            );
        }
        Ok(())
    }
}

/// Read a 64-bit value from a named warp lane using two 32-bit shuffles.
///
/// Operands are `[member_mask, value, lane_or_delta]`.
/// Lowering splits the value into two `b32` halves, shuffles both, and reassembles it.
#[pliron_op(
    name = "nvvm.shfl_sync_idx_i64",
    format,
    interfaces = [NOpdsInterface<3>, NResultsInterface<1>],
)]
pub struct ShflSyncIdxI64Op;

impl ShflSyncIdxI64Op {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }

    pub fn build(
        ctx: &mut Context,
        member_mask: Value,
        value: Value,
        lane_or_delta: Value,
    ) -> Ptr<Operation> {
        let result_ty: pliron::r#type::TypeHandle =
            IntegerType::get(ctx, 64, Signedness::Unsigned).into();
        Operation::new(
            ctx,
            Self::get_concrete_op_info(),
            vec![result_ty],
            vec![member_mask, value, lane_or_delta],
            vec![],
            0,
        )
    }
}

impl Verify for ShflSyncIdxI64Op {
    fn verify(&self, ctx: &Context) -> Result<(), Error> {
        let op = self.get_operation().deref(ctx);
        if op.get_num_operands() != 3 || op.get_num_results() != 1 {
            return verify_err!(
                op.loc(),
                "nvvm.shfl_sync_idx_i64 requires exactly [member_mask, value, lane_or_delta] and one result"
            );
        }
        if !is_i32(ctx, op.get_operand(0).get_type(ctx))
            || !is_i64(ctx, op.get_operand(1).get_type(ctx))
            || !is_i32(ctx, op.get_operand(2).get_type(ctx))
            || !is_i64(ctx, op.get_result(0).get_type(ctx))
        {
            return verify_err!(
                op.loc(),
                "nvvm.shfl_sync_idx_i64 requires i32 mask/lane and i64 value/result"
            );
        }
        Ok(())
    }
}

/// Read an f32 value from a lower named warp lane.
///
/// Operands are `[member_mask, value, lane_or_delta]`.
/// Generated lowering inserts the fixed clamp required by the selected shuffle mode.
#[pliron_op(
    name = "nvvm.shfl_sync_up_f32",
    format,
    interfaces = [NOpdsInterface<3>, NResultsInterface<1>],
)]
pub struct ShflSyncUpF32Op;

impl ShflSyncUpF32Op {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }

    pub fn build(
        ctx: &mut Context,
        member_mask: Value,
        value: Value,
        lane_or_delta: Value,
    ) -> Ptr<Operation> {
        let result_ty: pliron::r#type::TypeHandle = FP32Type::get(ctx).into();
        Operation::new(
            ctx,
            Self::get_concrete_op_info(),
            vec![result_ty],
            vec![member_mask, value, lane_or_delta],
            vec![],
            0,
        )
    }
}

impl Verify for ShflSyncUpF32Op {
    fn verify(&self, ctx: &Context) -> Result<(), Error> {
        let op = self.get_operation().deref(ctx);
        if op.get_num_operands() != 3 || op.get_num_results() != 1 {
            return verify_err!(
                op.loc(),
                "nvvm.shfl_sync_up_f32 requires exactly [member_mask, value, lane_or_delta] and one result"
            );
        }
        if !is_i32(ctx, op.get_operand(0).get_type(ctx))
            || !is_f32(ctx, op.get_operand(1).get_type(ctx))
            || !is_i32(ctx, op.get_operand(2).get_type(ctx))
            || !is_f32(ctx, op.get_result(0).get_type(ctx))
        {
            return verify_err!(
                op.loc(),
                "nvvm.shfl_sync_up_f32 requires i32 mask/lane and f32 value/result"
            );
        }
        Ok(())
    }
}

/// Read a 32-bit value from a lower named warp lane.
///
/// Operands are `[member_mask, value, lane_or_delta]`.
/// Generated lowering inserts the fixed clamp required by the selected shuffle mode.
#[pliron_op(
    name = "nvvm.shfl_sync_up_i32",
    format,
    interfaces = [NOpdsInterface<3>, NResultsInterface<1>],
)]
pub struct ShflSyncUpI32Op;

impl ShflSyncUpI32Op {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }

    pub fn build(
        ctx: &mut Context,
        member_mask: Value,
        value: Value,
        lane_or_delta: Value,
    ) -> Ptr<Operation> {
        let result_ty: pliron::r#type::TypeHandle =
            IntegerType::get(ctx, 32, Signedness::Unsigned).into();
        Operation::new(
            ctx,
            Self::get_concrete_op_info(),
            vec![result_ty],
            vec![member_mask, value, lane_or_delta],
            vec![],
            0,
        )
    }
}

impl Verify for ShflSyncUpI32Op {
    fn verify(&self, ctx: &Context) -> Result<(), Error> {
        let op = self.get_operation().deref(ctx);
        if op.get_num_operands() != 3 || op.get_num_results() != 1 {
            return verify_err!(
                op.loc(),
                "nvvm.shfl_sync_up_i32 requires exactly [member_mask, value, lane_or_delta] and one result"
            );
        }
        if !is_i32(ctx, op.get_operand(0).get_type(ctx))
            || !is_i32(ctx, op.get_operand(1).get_type(ctx))
            || !is_i32(ctx, op.get_operand(2).get_type(ctx))
            || !is_i32(ctx, op.get_result(0).get_type(ctx))
        {
            return verify_err!(
                op.loc(),
                "nvvm.shfl_sync_up_i32 requires i32 mask/lane and i32 value/result"
            );
        }
        Ok(())
    }
}

/// Read a 64-bit value from a lower named lane using two 32-bit shuffles.
///
/// Operands are `[member_mask, value, lane_or_delta]`.
/// Lowering splits the value into two `b32` halves, shuffles both, and reassembles it.
#[pliron_op(
    name = "nvvm.shfl_sync_up_i64",
    format,
    interfaces = [NOpdsInterface<3>, NResultsInterface<1>],
)]
pub struct ShflSyncUpI64Op;

impl ShflSyncUpI64Op {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }

    pub fn build(
        ctx: &mut Context,
        member_mask: Value,
        value: Value,
        lane_or_delta: Value,
    ) -> Ptr<Operation> {
        let result_ty: pliron::r#type::TypeHandle =
            IntegerType::get(ctx, 64, Signedness::Unsigned).into();
        Operation::new(
            ctx,
            Self::get_concrete_op_info(),
            vec![result_ty],
            vec![member_mask, value, lane_or_delta],
            vec![],
            0,
        )
    }
}

impl Verify for ShflSyncUpI64Op {
    fn verify(&self, ctx: &Context) -> Result<(), Error> {
        let op = self.get_operation().deref(ctx);
        if op.get_num_operands() != 3 || op.get_num_results() != 1 {
            return verify_err!(
                op.loc(),
                "nvvm.shfl_sync_up_i64 requires exactly [member_mask, value, lane_or_delta] and one result"
            );
        }
        if !is_i32(ctx, op.get_operand(0).get_type(ctx))
            || !is_i64(ctx, op.get_operand(1).get_type(ctx))
            || !is_i32(ctx, op.get_operand(2).get_type(ctx))
            || !is_i64(ctx, op.get_result(0).get_type(ctx))
        {
            return verify_err!(
                op.loc(),
                "nvvm.shfl_sync_up_i64 requires i32 mask/lane and i64 value/result"
            );
        }
        Ok(())
    }
}

/// Exchange an f32 value with a named XOR partner lane.
///
/// Operands are `[member_mask, value, lane_or_delta]`.
/// Generated lowering inserts the fixed clamp required by the selected shuffle mode.
#[pliron_op(
    name = "nvvm.shfl_sync_bfly_f32",
    format,
    interfaces = [NOpdsInterface<3>, NResultsInterface<1>],
)]
pub struct ShflSyncBflyF32Op;

impl ShflSyncBflyF32Op {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }

    pub fn build(
        ctx: &mut Context,
        member_mask: Value,
        value: Value,
        lane_or_delta: Value,
    ) -> Ptr<Operation> {
        let result_ty: pliron::r#type::TypeHandle = FP32Type::get(ctx).into();
        Operation::new(
            ctx,
            Self::get_concrete_op_info(),
            vec![result_ty],
            vec![member_mask, value, lane_or_delta],
            vec![],
            0,
        )
    }
}

impl Verify for ShflSyncBflyF32Op {
    fn verify(&self, ctx: &Context) -> Result<(), Error> {
        let op = self.get_operation().deref(ctx);
        if op.get_num_operands() != 3 || op.get_num_results() != 1 {
            return verify_err!(
                op.loc(),
                "nvvm.shfl_sync_bfly_f32 requires exactly [member_mask, value, lane_or_delta] and one result"
            );
        }
        if !is_i32(ctx, op.get_operand(0).get_type(ctx))
            || !is_f32(ctx, op.get_operand(1).get_type(ctx))
            || !is_i32(ctx, op.get_operand(2).get_type(ctx))
            || !is_f32(ctx, op.get_result(0).get_type(ctx))
        {
            return verify_err!(
                op.loc(),
                "nvvm.shfl_sync_bfly_f32 requires i32 mask/lane and f32 value/result"
            );
        }
        Ok(())
    }
}

/// Exchange a 32-bit value with a named XOR partner lane.
///
/// Operands are `[member_mask, value, lane_or_delta]`.
/// Generated lowering inserts the fixed clamp required by the selected shuffle mode.
#[pliron_op(
    name = "nvvm.shfl_sync_bfly_i32",
    format,
    interfaces = [NOpdsInterface<3>, NResultsInterface<1>],
)]
pub struct ShflSyncBflyI32Op;

impl ShflSyncBflyI32Op {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }

    pub fn build(
        ctx: &mut Context,
        member_mask: Value,
        value: Value,
        lane_or_delta: Value,
    ) -> Ptr<Operation> {
        let result_ty: pliron::r#type::TypeHandle =
            IntegerType::get(ctx, 32, Signedness::Unsigned).into();
        Operation::new(
            ctx,
            Self::get_concrete_op_info(),
            vec![result_ty],
            vec![member_mask, value, lane_or_delta],
            vec![],
            0,
        )
    }
}

impl Verify for ShflSyncBflyI32Op {
    fn verify(&self, ctx: &Context) -> Result<(), Error> {
        let op = self.get_operation().deref(ctx);
        if op.get_num_operands() != 3 || op.get_num_results() != 1 {
            return verify_err!(
                op.loc(),
                "nvvm.shfl_sync_bfly_i32 requires exactly [member_mask, value, lane_or_delta] and one result"
            );
        }
        if !is_i32(ctx, op.get_operand(0).get_type(ctx))
            || !is_i32(ctx, op.get_operand(1).get_type(ctx))
            || !is_i32(ctx, op.get_operand(2).get_type(ctx))
            || !is_i32(ctx, op.get_result(0).get_type(ctx))
        {
            return verify_err!(
                op.loc(),
                "nvvm.shfl_sync_bfly_i32 requires i32 mask/lane and i32 value/result"
            );
        }
        Ok(())
    }
}

/// Exchange a 64-bit value with a named XOR partner using two 32-bit shuffles.
///
/// Operands are `[member_mask, value, lane_or_delta]`.
/// Lowering splits the value into two `b32` halves, shuffles both, and reassembles it.
#[pliron_op(
    name = "nvvm.shfl_sync_bfly_i64",
    format,
    interfaces = [NOpdsInterface<3>, NResultsInterface<1>],
)]
pub struct ShflSyncBflyI64Op;

impl ShflSyncBflyI64Op {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }

    pub fn build(
        ctx: &mut Context,
        member_mask: Value,
        value: Value,
        lane_or_delta: Value,
    ) -> Ptr<Operation> {
        let result_ty: pliron::r#type::TypeHandle =
            IntegerType::get(ctx, 64, Signedness::Unsigned).into();
        Operation::new(
            ctx,
            Self::get_concrete_op_info(),
            vec![result_ty],
            vec![member_mask, value, lane_or_delta],
            vec![],
            0,
        )
    }
}

impl Verify for ShflSyncBflyI64Op {
    fn verify(&self, ctx: &Context) -> Result<(), Error> {
        let op = self.get_operation().deref(ctx);
        if op.get_num_operands() != 3 || op.get_num_results() != 1 {
            return verify_err!(
                op.loc(),
                "nvvm.shfl_sync_bfly_i64 requires exactly [member_mask, value, lane_or_delta] and one result"
            );
        }
        if !is_i32(ctx, op.get_operand(0).get_type(ctx))
            || !is_i64(ctx, op.get_operand(1).get_type(ctx))
            || !is_i32(ctx, op.get_operand(2).get_type(ctx))
            || !is_i64(ctx, op.get_result(0).get_type(ctx))
        {
            return verify_err!(
                op.loc(),
                "nvvm.shfl_sync_bfly_i64 requires i32 mask/lane and i64 value/result"
            );
        }
        Ok(())
    }
}

pub(super) fn register(ctx: &mut Context) {
    ShflSyncDownF32Op::register(ctx);
    ShflSyncDownI32Op::register(ctx);
    ShflSyncDownI64Op::register(ctx);
    ShflSyncIdxF32Op::register(ctx);
    ShflSyncIdxI32Op::register(ctx);
    ShflSyncIdxI64Op::register(ctx);
    ShflSyncUpF32Op::register(ctx);
    ShflSyncUpI32Op::register(ctx);
    ShflSyncUpI64Op::register(ctx);
    ShflSyncBflyF32Op::register(ctx);
    ShflSyncBflyI32Op::register(ctx);
    ShflSyncBflyI64Op::register(ctx);
}
