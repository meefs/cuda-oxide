/*
 * SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
 * SPDX-License-Identifier: Apache-2.0
 */

// @generated by cuda-intrinsics-gen 0.1.0. DO NOT EDIT.
// Catalog schema/version: 44/0.1.0; intrinsic ABI: v1; catalog SHA-256: 1f72d053300ad70672b9cf851743dd05e0904f7222828eecd53d5ca88a4a0088.
// LLVM source: 1cb4e3833c1919c2e6fb579a23ac0e2b22587b7e.

//! Structural operations for generated packed integer dot products.

use pliron::{
    builtin::{
        op_interfaces::{NOpdsInterface, NResultsInterface},
        types::{IntegerType, Signedness},
    },
    common_traits::Verify,
    context::{Context, Ptr},
    location::Located,
    op::Op,
    operation::Operation,
    result::Error,
    r#type::Typed,
    value::Value,
    verify_err,
};
use pliron_derive::pliron_op;

fn is_i32(ctx: &Context, ty: pliron::r#type::TypeHandle) -> bool {
    ty.deref(ctx)
        .downcast_ref::<IntegerType>()
        .is_some_and(|integer| integer.width() == 32)
}

/// Multiplies two packed signed halfwords by the low two signed bytes, sums them, and adds a signed accumulator.
/// Lowers to `dp2a.lo.s32.s32 %d, %a, %b, %c;`.
#[pliron_op(
    name = "nvvm.dp2a_s32",
    format,
    interfaces = [NOpdsInterface<3>, NResultsInterface<1>],
)]
pub struct Dp2aS32Op;

impl Dp2aS32Op {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }

    pub fn build(ctx: &mut Context, a: Value, b: Value, c: Value) -> Ptr<Operation> {
        let result_ty = IntegerType::get(ctx, 32, Signedness::Signed);
        Operation::new(
            ctx,
            Self::get_concrete_op_info(),
            vec![result_ty.into()],
            vec![a, b, c],
            vec![],
            0,
        )
    }
}

impl Verify for Dp2aS32Op {
    fn verify(&self, ctx: &Context) -> Result<(), Error> {
        let op = self.get_operation().deref(ctx);
        if op.get_num_operands() != 3 || op.get_num_results() != 1 {
            return verify_err!(
                op.loc(),
                "nvvm.dp2a_s32 requires exactly three operands and one result"
            );
        }
        if !(0..3).all(|index| is_i32(ctx, op.get_operand(index).get_type(ctx)))
            || !is_i32(ctx, op.get_result(0).get_type(ctx))
        {
            return verify_err!(
                op.loc(),
                "nvvm.dp2a_s32 operands and result must be 32-bit integers"
            );
        }
        Ok(())
    }
}

/// Multiplies two packed unsigned halfwords by the low two unsigned bytes, sums them, and adds an unsigned accumulator.
/// Lowers to `dp2a.lo.u32.u32 %d, %a, %b, %c;`.
#[pliron_op(
    name = "nvvm.dp2a_u32",
    format,
    interfaces = [NOpdsInterface<3>, NResultsInterface<1>],
)]
pub struct Dp2aU32Op;

impl Dp2aU32Op {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }

    pub fn build(ctx: &mut Context, a: Value, b: Value, c: Value) -> Ptr<Operation> {
        let result_ty = IntegerType::get(ctx, 32, Signedness::Unsigned);
        Operation::new(
            ctx,
            Self::get_concrete_op_info(),
            vec![result_ty.into()],
            vec![a, b, c],
            vec![],
            0,
        )
    }
}

impl Verify for Dp2aU32Op {
    fn verify(&self, ctx: &Context) -> Result<(), Error> {
        let op = self.get_operation().deref(ctx);
        if op.get_num_operands() != 3 || op.get_num_results() != 1 {
            return verify_err!(
                op.loc(),
                "nvvm.dp2a_u32 requires exactly three operands and one result"
            );
        }
        if !(0..3).all(|index| is_i32(ctx, op.get_operand(index).get_type(ctx)))
            || !is_i32(ctx, op.get_result(0).get_type(ctx))
        {
            return verify_err!(
                op.loc(),
                "nvvm.dp2a_u32 operands and result must be 32-bit integers"
            );
        }
        Ok(())
    }
}

/// Multiplies four pairs of packed signed bytes, sums them, and adds a signed accumulator.
/// Lowers to `dp4a.s32.s32 %d, %a, %b, %c;`.
#[pliron_op(
    name = "nvvm.dp4a_s32",
    format,
    interfaces = [NOpdsInterface<3>, NResultsInterface<1>],
)]
pub struct Dp4aS32Op;

impl Dp4aS32Op {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }

    pub fn build(ctx: &mut Context, a: Value, b: Value, c: Value) -> Ptr<Operation> {
        let result_ty = IntegerType::get(ctx, 32, Signedness::Signed);
        Operation::new(
            ctx,
            Self::get_concrete_op_info(),
            vec![result_ty.into()],
            vec![a, b, c],
            vec![],
            0,
        )
    }
}

impl Verify for Dp4aS32Op {
    fn verify(&self, ctx: &Context) -> Result<(), Error> {
        let op = self.get_operation().deref(ctx);
        if op.get_num_operands() != 3 || op.get_num_results() != 1 {
            return verify_err!(
                op.loc(),
                "nvvm.dp4a_s32 requires exactly three operands and one result"
            );
        }
        if !(0..3).all(|index| is_i32(ctx, op.get_operand(index).get_type(ctx)))
            || !is_i32(ctx, op.get_result(0).get_type(ctx))
        {
            return verify_err!(
                op.loc(),
                "nvvm.dp4a_s32 operands and result must be 32-bit integers"
            );
        }
        Ok(())
    }
}

/// Multiplies four pairs of packed unsigned bytes, sums them, and adds an unsigned accumulator.
/// Lowers to `dp4a.u32.u32 %d, %a, %b, %c;`.
#[pliron_op(
    name = "nvvm.dp4a_u32",
    format,
    interfaces = [NOpdsInterface<3>, NResultsInterface<1>],
)]
pub struct Dp4aU32Op;

impl Dp4aU32Op {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }

    pub fn build(ctx: &mut Context, a: Value, b: Value, c: Value) -> Ptr<Operation> {
        let result_ty = IntegerType::get(ctx, 32, Signedness::Unsigned);
        Operation::new(
            ctx,
            Self::get_concrete_op_info(),
            vec![result_ty.into()],
            vec![a, b, c],
            vec![],
            0,
        )
    }
}

impl Verify for Dp4aU32Op {
    fn verify(&self, ctx: &Context) -> Result<(), Error> {
        let op = self.get_operation().deref(ctx);
        if op.get_num_operands() != 3 || op.get_num_results() != 1 {
            return verify_err!(
                op.loc(),
                "nvvm.dp4a_u32 requires exactly three operands and one result"
            );
        }
        if !(0..3).all(|index| is_i32(ctx, op.get_operand(index).get_type(ctx)))
            || !is_i32(ctx, op.get_result(0).get_type(ctx))
        {
            return verify_err!(
                op.loc(),
                "nvvm.dp4a_u32 operands and result must be 32-bit integers"
            );
        }
        Ok(())
    }
}

pub(super) fn register(ctx: &mut Context) {
    Dp2aS32Op::register(ctx);
    Dp2aU32Op::register(ctx);
    Dp4aS32Op::register(ctx);
    Dp4aU32Op::register(ctx);
}
