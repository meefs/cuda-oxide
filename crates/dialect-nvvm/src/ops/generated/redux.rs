/*
 * SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
 * SPDX-License-Identifier: Apache-2.0
 */

// @generated by cuda-intrinsics-gen 0.1.0. DO NOT EDIT.
// Catalog schema/version: 44/0.1.0; intrinsic ABI: v1; catalog SHA-256: 1f72d053300ad70672b9cf851743dd05e0904f7222828eecd53d5ca88a4a0088.
// LLVM source: 1cb4e3833c1919c2e6fb579a23ac0e2b22587b7e.

//! Structural NVVM operations for the closed generated `redux.sync` family.

use pliron::{
    builtin::{
        op_interfaces::{NOpdsInterface, NResultsInterface},
        types::{IntegerType, Signedness},
    },
    common_traits::Verify,
    context::{Context, Ptr},
    location::Located,
    op::Op,
    operation::Operation,
    result::Error,
    r#type::Typed,
    value::Value,
    verify_err,
};
use pliron_derive::pliron_op;

fn is_i32(ctx: &Context, ty: pliron::r#type::TypeHandle) -> bool {
    ty.deref(ctx)
        .downcast_ref::<IntegerType>()
        .is_some_and(|integer| integer.width() == 32)
}

/// Synchronizes the named warp lanes, adds their 32-bit values modulo 2^32, and broadcasts the result to each participating lane.
///
/// Dialect operands are `[member_mask, value]`; generated lowering adapts
/// them to LLVM's `(value, member_mask)` signature.
#[pliron_op(
    name = "nvvm.redux_sync_add",
    format,
    interfaces = [NOpdsInterface<2>, NResultsInterface<1>],
)]
pub struct ReduxSyncAddOp;

impl ReduxSyncAddOp {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }

    pub fn build(ctx: &mut Context, member_mask: Value, value: Value) -> Ptr<Operation> {
        let result_ty = IntegerType::get(ctx, 32, Signedness::Unsigned);
        Operation::new(
            ctx,
            Self::get_concrete_op_info(),
            vec![result_ty.into()],
            vec![member_mask, value],
            vec![],
            0,
        )
    }
}

impl Verify for ReduxSyncAddOp {
    fn verify(&self, ctx: &Context) -> Result<(), Error> {
        let op = self.get_operation().deref(ctx);
        if op.get_num_operands() != 2 || op.get_num_results() != 1 {
            return verify_err!(
                op.loc(),
                "nvvm.redux_sync_add requires exactly two operands [member_mask, value] and one result"
            );
        }
        if !is_i32(ctx, op.get_operand(0).get_type(ctx))
            || !is_i32(ctx, op.get_operand(1).get_type(ctx))
            || !is_i32(ctx, op.get_result(0).get_type(ctx))
        {
            return verify_err!(
                op.loc(),
                "nvvm.redux_sync_add member mask, value, and result must be 32-bit integers"
            );
        }
        Ok(())
    }
}

/// Synchronizes the named warp lanes, reduces their 32-bit values with bitwise AND, and broadcasts the result.
///
/// Dialect operands are `[member_mask, value]`; generated lowering adapts
/// them to LLVM's `(value, member_mask)` signature.
#[pliron_op(
    name = "nvvm.redux_sync_and",
    format,
    interfaces = [NOpdsInterface<2>, NResultsInterface<1>],
)]
pub struct ReduxSyncAndOp;

impl ReduxSyncAndOp {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }

    pub fn build(ctx: &mut Context, member_mask: Value, value: Value) -> Ptr<Operation> {
        let result_ty = IntegerType::get(ctx, 32, Signedness::Unsigned);
        Operation::new(
            ctx,
            Self::get_concrete_op_info(),
            vec![result_ty.into()],
            vec![member_mask, value],
            vec![],
            0,
        )
    }
}

impl Verify for ReduxSyncAndOp {
    fn verify(&self, ctx: &Context) -> Result<(), Error> {
        let op = self.get_operation().deref(ctx);
        if op.get_num_operands() != 2 || op.get_num_results() != 1 {
            return verify_err!(
                op.loc(),
                "nvvm.redux_sync_and requires exactly two operands [member_mask, value] and one result"
            );
        }
        if !is_i32(ctx, op.get_operand(0).get_type(ctx))
            || !is_i32(ctx, op.get_operand(1).get_type(ctx))
            || !is_i32(ctx, op.get_result(0).get_type(ctx))
        {
            return verify_err!(
                op.loc(),
                "nvvm.redux_sync_and member mask, value, and result must be 32-bit integers"
            );
        }
        Ok(())
    }
}

/// Synchronizes the named warp lanes, selects their signed 32-bit maximum, and broadcasts it to each participating lane.
///
/// Dialect operands are `[member_mask, value]`; generated lowering adapts
/// them to LLVM's `(value, member_mask)` signature.
#[pliron_op(
    name = "nvvm.redux_sync_max",
    format,
    interfaces = [NOpdsInterface<2>, NResultsInterface<1>],
)]
pub struct ReduxSyncMaxOp;

impl ReduxSyncMaxOp {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }

    pub fn build(ctx: &mut Context, member_mask: Value, value: Value) -> Ptr<Operation> {
        let result_ty = IntegerType::get(ctx, 32, Signedness::Signed);
        Operation::new(
            ctx,
            Self::get_concrete_op_info(),
            vec![result_ty.into()],
            vec![member_mask, value],
            vec![],
            0,
        )
    }
}

impl Verify for ReduxSyncMaxOp {
    fn verify(&self, ctx: &Context) -> Result<(), Error> {
        let op = self.get_operation().deref(ctx);
        if op.get_num_operands() != 2 || op.get_num_results() != 1 {
            return verify_err!(
                op.loc(),
                "nvvm.redux_sync_max requires exactly two operands [member_mask, value] and one result"
            );
        }
        if !is_i32(ctx, op.get_operand(0).get_type(ctx))
            || !is_i32(ctx, op.get_operand(1).get_type(ctx))
            || !is_i32(ctx, op.get_result(0).get_type(ctx))
        {
            return verify_err!(
                op.loc(),
                "nvvm.redux_sync_max member mask, value, and result must be 32-bit integers"
            );
        }
        Ok(())
    }
}

/// Synchronizes the named warp lanes, selects their unsigned 32-bit maximum, and broadcasts it to each participating lane.
///
/// Dialect operands are `[member_mask, value]`; generated lowering adapts
/// them to LLVM's `(value, member_mask)` signature.
#[pliron_op(
    name = "nvvm.redux_sync_umax",
    format,
    interfaces = [NOpdsInterface<2>, NResultsInterface<1>],
)]
pub struct ReduxSyncUmaxOp;

impl ReduxSyncUmaxOp {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }

    pub fn build(ctx: &mut Context, member_mask: Value, value: Value) -> Ptr<Operation> {
        let result_ty = IntegerType::get(ctx, 32, Signedness::Unsigned);
        Operation::new(
            ctx,
            Self::get_concrete_op_info(),
            vec![result_ty.into()],
            vec![member_mask, value],
            vec![],
            0,
        )
    }
}

impl Verify for ReduxSyncUmaxOp {
    fn verify(&self, ctx: &Context) -> Result<(), Error> {
        let op = self.get_operation().deref(ctx);
        if op.get_num_operands() != 2 || op.get_num_results() != 1 {
            return verify_err!(
                op.loc(),
                "nvvm.redux_sync_umax requires exactly two operands [member_mask, value] and one result"
            );
        }
        if !is_i32(ctx, op.get_operand(0).get_type(ctx))
            || !is_i32(ctx, op.get_operand(1).get_type(ctx))
            || !is_i32(ctx, op.get_result(0).get_type(ctx))
        {
            return verify_err!(
                op.loc(),
                "nvvm.redux_sync_umax member mask, value, and result must be 32-bit integers"
            );
        }
        Ok(())
    }
}

/// Synchronizes the named warp lanes, selects their signed 32-bit minimum, and broadcasts it to each participating lane.
///
/// Dialect operands are `[member_mask, value]`; generated lowering adapts
/// them to LLVM's `(value, member_mask)` signature.
#[pliron_op(
    name = "nvvm.redux_sync_min",
    format,
    interfaces = [NOpdsInterface<2>, NResultsInterface<1>],
)]
pub struct ReduxSyncMinOp;

impl ReduxSyncMinOp {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }

    pub fn build(ctx: &mut Context, member_mask: Value, value: Value) -> Ptr<Operation> {
        let result_ty = IntegerType::get(ctx, 32, Signedness::Signed);
        Operation::new(
            ctx,
            Self::get_concrete_op_info(),
            vec![result_ty.into()],
            vec![member_mask, value],
            vec![],
            0,
        )
    }
}

impl Verify for ReduxSyncMinOp {
    fn verify(&self, ctx: &Context) -> Result<(), Error> {
        let op = self.get_operation().deref(ctx);
        if op.get_num_operands() != 2 || op.get_num_results() != 1 {
            return verify_err!(
                op.loc(),
                "nvvm.redux_sync_min requires exactly two operands [member_mask, value] and one result"
            );
        }
        if !is_i32(ctx, op.get_operand(0).get_type(ctx))
            || !is_i32(ctx, op.get_operand(1).get_type(ctx))
            || !is_i32(ctx, op.get_result(0).get_type(ctx))
        {
            return verify_err!(
                op.loc(),
                "nvvm.redux_sync_min member mask, value, and result must be 32-bit integers"
            );
        }
        Ok(())
    }
}

/// Synchronizes the named warp lanes, selects their unsigned 32-bit minimum, and broadcasts it to each participating lane.
///
/// Dialect operands are `[member_mask, value]`; generated lowering adapts
/// them to LLVM's `(value, member_mask)` signature.
#[pliron_op(
    name = "nvvm.redux_sync_umin",
    format,
    interfaces = [NOpdsInterface<2>, NResultsInterface<1>],
)]
pub struct ReduxSyncUminOp;

impl ReduxSyncUminOp {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }

    pub fn build(ctx: &mut Context, member_mask: Value, value: Value) -> Ptr<Operation> {
        let result_ty = IntegerType::get(ctx, 32, Signedness::Unsigned);
        Operation::new(
            ctx,
            Self::get_concrete_op_info(),
            vec![result_ty.into()],
            vec![member_mask, value],
            vec![],
            0,
        )
    }
}

impl Verify for ReduxSyncUminOp {
    fn verify(&self, ctx: &Context) -> Result<(), Error> {
        let op = self.get_operation().deref(ctx);
        if op.get_num_operands() != 2 || op.get_num_results() != 1 {
            return verify_err!(
                op.loc(),
                "nvvm.redux_sync_umin requires exactly two operands [member_mask, value] and one result"
            );
        }
        if !is_i32(ctx, op.get_operand(0).get_type(ctx))
            || !is_i32(ctx, op.get_operand(1).get_type(ctx))
            || !is_i32(ctx, op.get_result(0).get_type(ctx))
        {
            return verify_err!(
                op.loc(),
                "nvvm.redux_sync_umin member mask, value, and result must be 32-bit integers"
            );
        }
        Ok(())
    }
}

/// Synchronizes the named warp lanes, reduces their 32-bit values with bitwise OR, and broadcasts the result.
///
/// Dialect operands are `[member_mask, value]`; generated lowering adapts
/// them to LLVM's `(value, member_mask)` signature.
#[pliron_op(
    name = "nvvm.redux_sync_or",
    format,
    interfaces = [NOpdsInterface<2>, NResultsInterface<1>],
)]
pub struct ReduxSyncOrOp;

impl ReduxSyncOrOp {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }

    pub fn build(ctx: &mut Context, member_mask: Value, value: Value) -> Ptr<Operation> {
        let result_ty = IntegerType::get(ctx, 32, Signedness::Unsigned);
        Operation::new(
            ctx,
            Self::get_concrete_op_info(),
            vec![result_ty.into()],
            vec![member_mask, value],
            vec![],
            0,
        )
    }
}

impl Verify for ReduxSyncOrOp {
    fn verify(&self, ctx: &Context) -> Result<(), Error> {
        let op = self.get_operation().deref(ctx);
        if op.get_num_operands() != 2 || op.get_num_results() != 1 {
            return verify_err!(
                op.loc(),
                "nvvm.redux_sync_or requires exactly two operands [member_mask, value] and one result"
            );
        }
        if !is_i32(ctx, op.get_operand(0).get_type(ctx))
            || !is_i32(ctx, op.get_operand(1).get_type(ctx))
            || !is_i32(ctx, op.get_result(0).get_type(ctx))
        {
            return verify_err!(
                op.loc(),
                "nvvm.redux_sync_or member mask, value, and result must be 32-bit integers"
            );
        }
        Ok(())
    }
}

/// Synchronizes the named warp lanes, reduces their 32-bit values with bitwise XOR, and broadcasts the result.
///
/// Dialect operands are `[member_mask, value]`; generated lowering adapts
/// them to LLVM's `(value, member_mask)` signature.
#[pliron_op(
    name = "nvvm.redux_sync_xor",
    format,
    interfaces = [NOpdsInterface<2>, NResultsInterface<1>],
)]
pub struct ReduxSyncXorOp;

impl ReduxSyncXorOp {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }

    pub fn build(ctx: &mut Context, member_mask: Value, value: Value) -> Ptr<Operation> {
        let result_ty = IntegerType::get(ctx, 32, Signedness::Unsigned);
        Operation::new(
            ctx,
            Self::get_concrete_op_info(),
            vec![result_ty.into()],
            vec![member_mask, value],
            vec![],
            0,
        )
    }
}

impl Verify for ReduxSyncXorOp {
    fn verify(&self, ctx: &Context) -> Result<(), Error> {
        let op = self.get_operation().deref(ctx);
        if op.get_num_operands() != 2 || op.get_num_results() != 1 {
            return verify_err!(
                op.loc(),
                "nvvm.redux_sync_xor requires exactly two operands [member_mask, value] and one result"
            );
        }
        if !is_i32(ctx, op.get_operand(0).get_type(ctx))
            || !is_i32(ctx, op.get_operand(1).get_type(ctx))
            || !is_i32(ctx, op.get_result(0).get_type(ctx))
        {
            return verify_err!(
                op.loc(),
                "nvvm.redux_sync_xor member mask, value, and result must be 32-bit integers"
            );
        }
        Ok(())
    }
}

pub(super) fn register(ctx: &mut Context) {
    ReduxSyncAddOp::register(ctx);
    ReduxSyncAndOp::register(ctx);
    ReduxSyncMaxOp::register(ctx);
    ReduxSyncUmaxOp::register(ctx);
    ReduxSyncMinOp::register(ctx);
    ReduxSyncUminOp::register(ctx);
    ReduxSyncOrOp::register(ctx);
    ReduxSyncXorOp::register(ctx);
}
