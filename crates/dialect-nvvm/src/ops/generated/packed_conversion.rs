/*
 * SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
 * SPDX-License-Identifier: Apache-2.0
 */

// @generated by cuda-intrinsics-gen 0.1.0. DO NOT EDIT.
// Catalog schema/version: 44/0.1.0; intrinsic ABI: v1; catalog SHA-256: 1f72d053300ad70672b9cf851743dd05e0904f7222828eecd53d5ca88a4a0088.
// LLVM source: 1cb4e3833c1919c2e6fb579a23ac0e2b22587b7e.

//! Structural operation for generated packed conversion.

use pliron::{
    builtin::{
        op_interfaces::{NOpdsInterface, NResultsInterface},
        types::{FP32Type, IntegerType, Signedness},
    },
    common_traits::Verify,
    context::{Context, Ptr},
    location::Located,
    op::Op,
    operation::Operation,
    result::Error,
    r#type::Typed,
    value::Value,
    verify_err,
};
use pliron_derive::pliron_op;

fn is_f32(ctx: &Context, ty: pliron::r#type::TypeHandle) -> bool {
    ty.deref(ctx).downcast_ref::<FP32Type>().is_some()
}

fn is_integer_width(ctx: &Context, ty: pliron::r#type::TypeHandle, width: u32) -> bool {
    ty.deref(ctx)
        .downcast_ref::<IntegerType>()
        .is_some_and(|integer| integer.width() == width)
}

/// Converts two f32 values to packed f16x2 with nearest-even rounding and the first argument in the low half.
///
/// The first input becomes the low f16 lane; the second becomes the high lane.
#[pliron_op(
    name = "nvvm.cvt_f16x2_f32",
    format,
    interfaces = [NOpdsInterface<2>, NResultsInterface<1>],
)]
pub struct CvtF16x2F32Op;

impl CvtF16x2F32Op {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }

    pub fn build(ctx: &mut Context, low: Value, high: Value) -> Ptr<Operation> {
        let result_ty = IntegerType::get(ctx, 32, Signedness::Unsigned);
        Operation::new(
            ctx,
            Self::get_concrete_op_info(),
            vec![result_ty.into()],
            vec![low, high],
            vec![],
            0,
        )
    }
}

impl Verify for CvtF16x2F32Op {
    fn verify(&self, ctx: &Context) -> Result<(), Error> {
        let op = self.get_operation().deref(ctx);
        if op.get_num_operands() != 2 || op.get_num_results() != 1 {
            return verify_err!(
                op.loc(),
                "nvvm.cvt_f16x2_f32 requires two operands and one result"
            );
        }
        if !is_f32(ctx, op.get_operand(0).get_type(ctx))
            || !is_f32(ctx, op.get_operand(1).get_type(ctx))
            || !is_integer_width(ctx, op.get_result(0).get_type(ctx), 32)
        {
            return verify_err!(
                op.loc(),
                "nvvm.cvt_f16x2_f32 requires f32 operands and one 32-bit integer result"
            );
        }
        Ok(())
    }
}

/// Converts two f32 values to packed bf16x2 with the first argument in the low half.
///
/// The first input becomes the low bf16 lane; the second becomes the high lane.
#[pliron_op(
    name = "nvvm.cvt_f32x2_bf16x2",
    format,
    interfaces = [NOpdsInterface<2>, NResultsInterface<1>],
)]
pub struct CvtF32x2Bf16x2Op;

impl CvtF32x2Bf16x2Op {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }

    pub fn build(ctx: &mut Context, low: Value, high: Value) -> Ptr<Operation> {
        let result_ty = IntegerType::get(ctx, 32, Signedness::Unsigned);
        Operation::new(
            ctx,
            Self::get_concrete_op_info(),
            vec![result_ty.into()],
            vec![low, high],
            vec![],
            0,
        )
    }
}

impl Verify for CvtF32x2Bf16x2Op {
    fn verify(&self, ctx: &Context) -> Result<(), Error> {
        let op = self.get_operation().deref(ctx);
        if op.get_num_operands() != 2 || op.get_num_results() != 1 {
            return verify_err!(
                op.loc(),
                "nvvm.cvt_f32x2_bf16x2 requires two operands and one result"
            );
        }
        if !is_f32(ctx, op.get_operand(0).get_type(ctx))
            || !is_f32(ctx, op.get_operand(1).get_type(ctx))
            || !is_integer_width(ctx, op.get_result(0).get_type(ctx), 32)
        {
            return verify_err!(
                op.loc(),
                "nvvm.cvt_f32x2_bf16x2 requires f32 operands and one 32-bit integer result"
            );
        }
        Ok(())
    }
}

/// Converts two f32 values to packed bf16x2 with nearest-even rounding, ReLU, and the first argument in the low half.
///
/// The first input becomes the low bf16 lane; the second becomes the high lane.
#[pliron_op(
    name = "nvvm.cvt_rn_relu_bf16x2_f32",
    format,
    interfaces = [NOpdsInterface<2>, NResultsInterface<1>],
)]
pub struct CvtRnReluBf16x2F32Op;

impl CvtRnReluBf16x2F32Op {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }

    pub fn build(ctx: &mut Context, low: Value, high: Value) -> Ptr<Operation> {
        let result_ty = IntegerType::get(ctx, 32, Signedness::Unsigned);
        Operation::new(
            ctx,
            Self::get_concrete_op_info(),
            vec![result_ty.into()],
            vec![low, high],
            vec![],
            0,
        )
    }
}

impl Verify for CvtRnReluBf16x2F32Op {
    fn verify(&self, ctx: &Context) -> Result<(), Error> {
        let op = self.get_operation().deref(ctx);
        if op.get_num_operands() != 2 || op.get_num_results() != 1 {
            return verify_err!(
                op.loc(),
                "nvvm.cvt_rn_relu_bf16x2_f32 requires two operands and one result"
            );
        }
        if !is_f32(ctx, op.get_operand(0).get_type(ctx))
            || !is_f32(ctx, op.get_operand(1).get_type(ctx))
            || !is_integer_width(ctx, op.get_result(0).get_type(ctx), 32)
        {
            return verify_err!(
                op.loc(),
                "nvvm.cvt_rn_relu_bf16x2_f32 requires f32 operands and one 32-bit integer result"
            );
        }
        Ok(())
    }
}

/// Converts two f32 values to packed f16x2 with nearest-even rounding, ReLU, and the first argument in the low half.
///
/// The first input becomes the low f16 lane; the second becomes the high lane.
#[pliron_op(
    name = "nvvm.cvt_rn_relu_f16x2_f32",
    format,
    interfaces = [NOpdsInterface<2>, NResultsInterface<1>],
)]
pub struct CvtRnReluF16x2F32Op;

impl CvtRnReluF16x2F32Op {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }

    pub fn build(ctx: &mut Context, low: Value, high: Value) -> Ptr<Operation> {
        let result_ty = IntegerType::get(ctx, 32, Signedness::Unsigned);
        Operation::new(
            ctx,
            Self::get_concrete_op_info(),
            vec![result_ty.into()],
            vec![low, high],
            vec![],
            0,
        )
    }
}

impl Verify for CvtRnReluF16x2F32Op {
    fn verify(&self, ctx: &Context) -> Result<(), Error> {
        let op = self.get_operation().deref(ctx);
        if op.get_num_operands() != 2 || op.get_num_results() != 1 {
            return verify_err!(
                op.loc(),
                "nvvm.cvt_rn_relu_f16x2_f32 requires two operands and one result"
            );
        }
        if !is_f32(ctx, op.get_operand(0).get_type(ctx))
            || !is_f32(ctx, op.get_operand(1).get_type(ctx))
            || !is_integer_width(ctx, op.get_result(0).get_type(ctx), 32)
        {
            return verify_err!(
                op.loc(),
                "nvvm.cvt_rn_relu_f16x2_f32 requires f32 operands and one 32-bit integer result"
            );
        }
        Ok(())
    }
}

/// Converts two f32 values to packed e4m3x2 with nearest-even finite saturation and the first argument in the low byte.
///
/// The first input becomes the low e4m3 lane; the second becomes the high lane.
#[pliron_op(
    name = "nvvm.cvt_rn_satfinite_e4m3x2_f32",
    format,
    interfaces = [NOpdsInterface<2>, NResultsInterface<1>],
)]
pub struct CvtRnSatfiniteE4m3x2F32Op;

impl CvtRnSatfiniteE4m3x2F32Op {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }

    pub fn build(ctx: &mut Context, low: Value, high: Value) -> Ptr<Operation> {
        let result_ty = IntegerType::get(ctx, 16, Signedness::Unsigned);
        Operation::new(
            ctx,
            Self::get_concrete_op_info(),
            vec![result_ty.into()],
            vec![low, high],
            vec![],
            0,
        )
    }
}

impl Verify for CvtRnSatfiniteE4m3x2F32Op {
    fn verify(&self, ctx: &Context) -> Result<(), Error> {
        let op = self.get_operation().deref(ctx);
        if op.get_num_operands() != 2 || op.get_num_results() != 1 {
            return verify_err!(
                op.loc(),
                "nvvm.cvt_rn_satfinite_e4m3x2_f32 requires two operands and one result"
            );
        }
        if !is_f32(ctx, op.get_operand(0).get_type(ctx))
            || !is_f32(ctx, op.get_operand(1).get_type(ctx))
            || !is_integer_width(ctx, op.get_result(0).get_type(ctx), 16)
        {
            return verify_err!(
                op.loc(),
                "nvvm.cvt_rn_satfinite_e4m3x2_f32 requires f32 operands and one 16-bit integer result"
            );
        }
        Ok(())
    }
}

/// Converts two f32 values to packed e5m2x2 with nearest-even finite saturation and the first argument in the low byte.
///
/// The first input becomes the low e5m2 lane; the second becomes the high lane.
#[pliron_op(
    name = "nvvm.cvt_rn_satfinite_e5m2x2_f32",
    format,
    interfaces = [NOpdsInterface<2>, NResultsInterface<1>],
)]
pub struct CvtRnSatfiniteE5m2x2F32Op;

impl CvtRnSatfiniteE5m2x2F32Op {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }

    pub fn build(ctx: &mut Context, low: Value, high: Value) -> Ptr<Operation> {
        let result_ty = IntegerType::get(ctx, 16, Signedness::Unsigned);
        Operation::new(
            ctx,
            Self::get_concrete_op_info(),
            vec![result_ty.into()],
            vec![low, high],
            vec![],
            0,
        )
    }
}

impl Verify for CvtRnSatfiniteE5m2x2F32Op {
    fn verify(&self, ctx: &Context) -> Result<(), Error> {
        let op = self.get_operation().deref(ctx);
        if op.get_num_operands() != 2 || op.get_num_results() != 1 {
            return verify_err!(
                op.loc(),
                "nvvm.cvt_rn_satfinite_e5m2x2_f32 requires two operands and one result"
            );
        }
        if !is_f32(ctx, op.get_operand(0).get_type(ctx))
            || !is_f32(ctx, op.get_operand(1).get_type(ctx))
            || !is_integer_width(ctx, op.get_result(0).get_type(ctx), 16)
        {
            return verify_err!(
                op.loc(),
                "nvvm.cvt_rn_satfinite_e5m2x2_f32 requires f32 operands and one 16-bit integer result"
            );
        }
        Ok(())
    }
}

/// Converts two f32 values to packed e4m3x2 with nearest-even finite saturation, ReLU, and the first argument in the low byte.
///
/// The first input becomes the low e4m3 lane; the second becomes the high lane.
#[pliron_op(
    name = "nvvm.cvt_rn_satfinite_relu_e4m3x2_f32",
    format,
    interfaces = [NOpdsInterface<2>, NResultsInterface<1>],
)]
pub struct CvtRnSatfiniteReluE4m3x2F32Op;

impl CvtRnSatfiniteReluE4m3x2F32Op {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }

    pub fn build(ctx: &mut Context, low: Value, high: Value) -> Ptr<Operation> {
        let result_ty = IntegerType::get(ctx, 16, Signedness::Unsigned);
        Operation::new(
            ctx,
            Self::get_concrete_op_info(),
            vec![result_ty.into()],
            vec![low, high],
            vec![],
            0,
        )
    }
}

impl Verify for CvtRnSatfiniteReluE4m3x2F32Op {
    fn verify(&self, ctx: &Context) -> Result<(), Error> {
        let op = self.get_operation().deref(ctx);
        if op.get_num_operands() != 2 || op.get_num_results() != 1 {
            return verify_err!(
                op.loc(),
                "nvvm.cvt_rn_satfinite_relu_e4m3x2_f32 requires two operands and one result"
            );
        }
        if !is_f32(ctx, op.get_operand(0).get_type(ctx))
            || !is_f32(ctx, op.get_operand(1).get_type(ctx))
            || !is_integer_width(ctx, op.get_result(0).get_type(ctx), 16)
        {
            return verify_err!(
                op.loc(),
                "nvvm.cvt_rn_satfinite_relu_e4m3x2_f32 requires f32 operands and one 16-bit integer result"
            );
        }
        Ok(())
    }
}

/// Converts two f32 values to packed e5m2x2 with nearest-even finite saturation, ReLU, and the first argument in the low byte.
///
/// The first input becomes the low e5m2 lane; the second becomes the high lane.
#[pliron_op(
    name = "nvvm.cvt_rn_satfinite_relu_e5m2x2_f32",
    format,
    interfaces = [NOpdsInterface<2>, NResultsInterface<1>],
)]
pub struct CvtRnSatfiniteReluE5m2x2F32Op;

impl CvtRnSatfiniteReluE5m2x2F32Op {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }

    pub fn build(ctx: &mut Context, low: Value, high: Value) -> Ptr<Operation> {
        let result_ty = IntegerType::get(ctx, 16, Signedness::Unsigned);
        Operation::new(
            ctx,
            Self::get_concrete_op_info(),
            vec![result_ty.into()],
            vec![low, high],
            vec![],
            0,
        )
    }
}

impl Verify for CvtRnSatfiniteReluE5m2x2F32Op {
    fn verify(&self, ctx: &Context) -> Result<(), Error> {
        let op = self.get_operation().deref(ctx);
        if op.get_num_operands() != 2 || op.get_num_results() != 1 {
            return verify_err!(
                op.loc(),
                "nvvm.cvt_rn_satfinite_relu_e5m2x2_f32 requires two operands and one result"
            );
        }
        if !is_f32(ctx, op.get_operand(0).get_type(ctx))
            || !is_f32(ctx, op.get_operand(1).get_type(ctx))
            || !is_integer_width(ctx, op.get_result(0).get_type(ctx), 16)
        {
            return verify_err!(
                op.loc(),
                "nvvm.cvt_rn_satfinite_relu_e5m2x2_f32 requires f32 operands and one 16-bit integer result"
            );
        }
        Ok(())
    }
}

/// Converts two f32 values to packed bf16x2 with toward-zero rounding and the first argument in the low half.
///
/// The first input becomes the low bf16 lane; the second becomes the high lane.
#[pliron_op(
    name = "nvvm.cvt_rz_bf16x2_f32",
    format,
    interfaces = [NOpdsInterface<2>, NResultsInterface<1>],
)]
pub struct CvtRzBf16x2F32Op;

impl CvtRzBf16x2F32Op {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }

    pub fn build(ctx: &mut Context, low: Value, high: Value) -> Ptr<Operation> {
        let result_ty = IntegerType::get(ctx, 32, Signedness::Unsigned);
        Operation::new(
            ctx,
            Self::get_concrete_op_info(),
            vec![result_ty.into()],
            vec![low, high],
            vec![],
            0,
        )
    }
}

impl Verify for CvtRzBf16x2F32Op {
    fn verify(&self, ctx: &Context) -> Result<(), Error> {
        let op = self.get_operation().deref(ctx);
        if op.get_num_operands() != 2 || op.get_num_results() != 1 {
            return verify_err!(
                op.loc(),
                "nvvm.cvt_rz_bf16x2_f32 requires two operands and one result"
            );
        }
        if !is_f32(ctx, op.get_operand(0).get_type(ctx))
            || !is_f32(ctx, op.get_operand(1).get_type(ctx))
            || !is_integer_width(ctx, op.get_result(0).get_type(ctx), 32)
        {
            return verify_err!(
                op.loc(),
                "nvvm.cvt_rz_bf16x2_f32 requires f32 operands and one 32-bit integer result"
            );
        }
        Ok(())
    }
}

/// Converts two f32 values to packed f16x2 with toward-zero rounding and the first argument in the low half.
///
/// The first input becomes the low f16 lane; the second becomes the high lane.
#[pliron_op(
    name = "nvvm.cvt_rz_f16x2_f32",
    format,
    interfaces = [NOpdsInterface<2>, NResultsInterface<1>],
)]
pub struct CvtRzF16x2F32Op;

impl CvtRzF16x2F32Op {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }

    pub fn build(ctx: &mut Context, low: Value, high: Value) -> Ptr<Operation> {
        let result_ty = IntegerType::get(ctx, 32, Signedness::Unsigned);
        Operation::new(
            ctx,
            Self::get_concrete_op_info(),
            vec![result_ty.into()],
            vec![low, high],
            vec![],
            0,
        )
    }
}

impl Verify for CvtRzF16x2F32Op {
    fn verify(&self, ctx: &Context) -> Result<(), Error> {
        let op = self.get_operation().deref(ctx);
        if op.get_num_operands() != 2 || op.get_num_results() != 1 {
            return verify_err!(
                op.loc(),
                "nvvm.cvt_rz_f16x2_f32 requires two operands and one result"
            );
        }
        if !is_f32(ctx, op.get_operand(0).get_type(ctx))
            || !is_f32(ctx, op.get_operand(1).get_type(ctx))
            || !is_integer_width(ctx, op.get_result(0).get_type(ctx), 32)
        {
            return verify_err!(
                op.loc(),
                "nvvm.cvt_rz_f16x2_f32 requires f32 operands and one 32-bit integer result"
            );
        }
        Ok(())
    }
}

pub(super) fn register(ctx: &mut Context) {
    CvtF16x2F32Op::register(ctx);
    CvtF32x2Bf16x2Op::register(ctx);
    CvtRnReluBf16x2F32Op::register(ctx);
    CvtRnReluF16x2F32Op::register(ctx);
    CvtRnSatfiniteE4m3x2F32Op::register(ctx);
    CvtRnSatfiniteE5m2x2F32Op::register(ctx);
    CvtRnSatfiniteReluE4m3x2F32Op::register(ctx);
    CvtRnSatfiniteReluE5m2x2F32Op::register(ctx);
    CvtRzBf16x2F32Op::register(ctx);
    CvtRzF16x2F32Op::register(ctx);
}
