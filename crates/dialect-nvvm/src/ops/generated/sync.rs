/*
 * SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
 * SPDX-License-Identifier: Apache-2.0
 */

// @generated by cuda-intrinsics-gen 0.1.0. DO NOT EDIT.
// Catalog schema/version: 44/0.1.0; intrinsic ABI: v1; catalog SHA-256: 1f72d053300ad70672b9cf851743dd05e0904f7222828eecd53d5ca88a4a0088.
// LLVM source: 1cb4e3833c1919c2e6fb579a23ac0e2b22587b7e.

//! Structural operation for generated CTA synchronization.

use pliron::{
    builtin::op_interfaces::{NOpdsInterface, NResultsInterface},
    context::{Context, Ptr},
    op::Op,
    operation::Operation,
};
use pliron_derive::pliron_op;

/// Waits until every active thread in the CTA reaches the same barrier, then makes their earlier memory accesses visible to the CTA.
#[pliron_op(
    name = "nvvm.barrier0",
    format,
    verifier = "succ",
    interfaces = [NOpdsInterface<0>, NResultsInterface<0>],
)]
pub struct Barrier0Op;

impl Barrier0Op {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }
}

/// Orders this thread's memory operations for observers on its GPU.
#[pliron_op(
    name = "nvvm.threadfence",
    format,
    verifier = "succ",
    interfaces = [NOpdsInterface<0>, NResultsInterface<0>],
)]
pub struct ThreadfenceOp;

impl ThreadfenceOp {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }
}

/// Orders this thread's memory operations for observers in its CTA.
#[pliron_op(
    name = "nvvm.threadfence_block",
    format,
    verifier = "succ",
    interfaces = [NOpdsInterface<0>, NResultsInterface<0>],
)]
pub struct ThreadfenceBlockOp;

impl ThreadfenceBlockOp {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }
}

/// Orders this thread's memory operations for system-wide observers.
#[pliron_op(
    name = "nvvm.threadfence_system",
    format,
    verifier = "succ",
    interfaces = [NOpdsInterface<0>, NResultsInterface<0>],
)]
pub struct ThreadfenceSystemOp;

impl ThreadfenceSystemOp {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }
}

pub(super) fn register(ctx: &mut Context) {
    Barrier0Op::register(ctx);
    ThreadfenceOp::register(ctx);
    ThreadfenceBlockOp::register(ctx);
    ThreadfenceSystemOp::register(ctx);
}
