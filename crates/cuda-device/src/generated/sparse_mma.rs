/*
 * SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
 * SPDX-License-Identifier: Apache-2.0
 */

// @generated by cuda-intrinsics-gen 0.1.0. DO NOT EDIT.
// Catalog schema/version: 44/0.1.0; intrinsic ABI: v1; catalog SHA-256: 1f72d053300ad70672b9cf851743dd05e0904f7222828eecd53d5ca88a4a0088.
// LLVM source: 1cb4e3833c1919c2e6fb579a23ac0e2b22587b7e.

// Included inside `cuda_device::wmma` to keep public paths stable.

/// Multiplies warp-distributed sparse signed A and signed B INT4 fragments and adds a wrapping s32 accumulator.
/// Lowers to `mma.sp.sync.aligned.m16n8k128.row.col.s32.s4.s4.s32`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 7.1 and `sm_80+`.
/// Signed accumulator overflow wraps.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 4-, 4-, and 4-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must encode two distinct 2-bit indices; `0x0`, `0x5`, `0xa`, and `0xf` are undefined behavior.
/// `selector` must be the compile-time constant `0`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_m16n8k128_s32_s4(
    c: [i32; 4],
    a: [u32; 4],
    b: [u32; 4],
    metadata: u32,
    selector: u32,
) -> [i32; 4] {
    let _ = (c, a, b, metadata, selector);
    unreachable!("mma_sp_m16n8k128_s32_s4 called outside CUDA kernel context")
}

/// Multiplies warp-distributed sparse signed A and signed B INT4 fragments and adds a saturating s32 accumulator.
/// Lowers to `mma.sp.sync.aligned.m16n8k128.row.col.satfinite.s32.s4.s4.s32`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 7.1 and `sm_80+`.
/// Signed accumulator overflow clamps to the finite `i32` range.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 4-, 4-, and 4-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must encode two distinct 2-bit indices; `0x0`, `0x5`, `0xa`, and `0xf` are undefined behavior.
/// `selector` must be the compile-time constant `0`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_m16n8k128_s32_s4_satfinite(
    c: [i32; 4],
    a: [u32; 4],
    b: [u32; 4],
    metadata: u32,
    selector: u32,
) -> [i32; 4] {
    let _ = (c, a, b, metadata, selector);
    unreachable!("mma_sp_m16n8k128_s32_s4_satfinite called outside CUDA kernel context")
}

/// Multiplies warp-distributed sparse signed A and unsigned B INT4 fragments and adds a wrapping s32 accumulator.
/// Lowers to `mma.sp.sync.aligned.m16n8k128.row.col.s32.s4.u4.s32`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 7.1 and `sm_80+`.
/// Signed accumulator overflow wraps.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 4-, 4-, and 4-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must encode two distinct 2-bit indices; `0x0`, `0x5`, `0xa`, and `0xf` are undefined behavior.
/// `selector` must be the compile-time constant `0`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_m16n8k128_s32_s4_u4(
    c: [i32; 4],
    a: [u32; 4],
    b: [u32; 4],
    metadata: u32,
    selector: u32,
) -> [i32; 4] {
    let _ = (c, a, b, metadata, selector);
    unreachable!("mma_sp_m16n8k128_s32_s4_u4 called outside CUDA kernel context")
}

/// Multiplies warp-distributed sparse signed A and unsigned B INT4 fragments and adds a saturating s32 accumulator.
/// Lowers to `mma.sp.sync.aligned.m16n8k128.row.col.satfinite.s32.s4.u4.s32`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 7.1 and `sm_80+`.
/// Signed accumulator overflow clamps to the finite `i32` range.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 4-, 4-, and 4-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must encode two distinct 2-bit indices; `0x0`, `0x5`, `0xa`, and `0xf` are undefined behavior.
/// `selector` must be the compile-time constant `0`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_m16n8k128_s32_s4_u4_satfinite(
    c: [i32; 4],
    a: [u32; 4],
    b: [u32; 4],
    metadata: u32,
    selector: u32,
) -> [i32; 4] {
    let _ = (c, a, b, metadata, selector);
    unreachable!("mma_sp_m16n8k128_s32_s4_u4_satfinite called outside CUDA kernel context")
}

/// Multiplies warp-distributed sparse unsigned A and unsigned B INT4 fragments and adds a wrapping s32 accumulator.
/// Lowers to `mma.sp.sync.aligned.m16n8k128.row.col.s32.u4.u4.s32`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 7.1 and `sm_80+`.
/// Signed accumulator overflow wraps.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 4-, 4-, and 4-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must encode two distinct 2-bit indices; `0x0`, `0x5`, `0xa`, and `0xf` are undefined behavior.
/// `selector` must be the compile-time constant `0`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_m16n8k128_s32_u4(
    c: [i32; 4],
    a: [u32; 4],
    b: [u32; 4],
    metadata: u32,
    selector: u32,
) -> [i32; 4] {
    let _ = (c, a, b, metadata, selector);
    unreachable!("mma_sp_m16n8k128_s32_u4 called outside CUDA kernel context")
}

/// Multiplies warp-distributed sparse unsigned A and signed B INT4 fragments and adds a wrapping s32 accumulator.
/// Lowers to `mma.sp.sync.aligned.m16n8k128.row.col.s32.u4.s4.s32`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 7.1 and `sm_80+`.
/// Signed accumulator overflow wraps.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 4-, 4-, and 4-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must encode two distinct 2-bit indices; `0x0`, `0x5`, `0xa`, and `0xf` are undefined behavior.
/// `selector` must be the compile-time constant `0`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_m16n8k128_s32_u4_s4(
    c: [i32; 4],
    a: [u32; 4],
    b: [u32; 4],
    metadata: u32,
    selector: u32,
) -> [i32; 4] {
    let _ = (c, a, b, metadata, selector);
    unreachable!("mma_sp_m16n8k128_s32_u4_s4 called outside CUDA kernel context")
}

/// Multiplies warp-distributed sparse unsigned A and signed B INT4 fragments and adds a saturating s32 accumulator.
/// Lowers to `mma.sp.sync.aligned.m16n8k128.row.col.satfinite.s32.u4.s4.s32`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 7.1 and `sm_80+`.
/// Signed accumulator overflow clamps to the finite `i32` range.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 4-, 4-, and 4-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must encode two distinct 2-bit indices; `0x0`, `0x5`, `0xa`, and `0xf` are undefined behavior.
/// `selector` must be the compile-time constant `0`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_m16n8k128_s32_u4_s4_satfinite(
    c: [i32; 4],
    a: [u32; 4],
    b: [u32; 4],
    metadata: u32,
    selector: u32,
) -> [i32; 4] {
    let _ = (c, a, b, metadata, selector);
    unreachable!("mma_sp_m16n8k128_s32_u4_s4_satfinite called outside CUDA kernel context")
}

/// Multiplies warp-distributed sparse unsigned A and unsigned B INT4 fragments and adds a saturating s32 accumulator.
/// Lowers to `mma.sp.sync.aligned.m16n8k128.row.col.satfinite.s32.u4.u4.s32`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 7.1 and `sm_80+`.
/// Signed accumulator overflow clamps to the finite `i32` range.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 4-, 4-, and 4-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must encode two distinct 2-bit indices; `0x0`, `0x5`, `0xa`, and `0xf` are undefined behavior.
/// `selector` must be the compile-time constant `0`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_m16n8k128_s32_u4_satfinite(
    c: [i32; 4],
    a: [u32; 4],
    b: [u32; 4],
    metadata: u32,
    selector: u32,
) -> [i32; 4] {
    let _ = (c, a, b, metadata, selector);
    unreachable!("mma_sp_m16n8k128_s32_u4_satfinite called outside CUDA kernel context")
}

/// Multiplies warp-distributed sparse signed A and signed B INT8 fragments and adds a wrapping s32 accumulator.
/// Lowers to `mma.sp.sync.aligned.m16n8k32.row.col.s32.s8.s8.s32`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 7.1 and `sm_80+`.
/// Signed accumulator overflow wraps.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 4-, 2-, and 2-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must encode two distinct 2-bit indices; `0x0`, `0x5`, `0xa`, and `0xf` are undefined behavior.
/// `selector` must be the compile-time constant `0` or `1`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_m16n8k32_s32_s8(
    c: [i32; 4],
    a: [u32; 2],
    b: [u32; 2],
    metadata: u32,
    selector: u32,
) -> [i32; 4] {
    let _ = (c, a, b, metadata, selector);
    unreachable!("mma_sp_m16n8k32_s32_s8 called outside CUDA kernel context")
}

/// Multiplies warp-distributed sparse signed A and signed B INT8 fragments and adds a saturating s32 accumulator.
/// Lowers to `mma.sp.sync.aligned.m16n8k32.row.col.satfinite.s32.s8.s8.s32`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 7.1 and `sm_80+`.
/// Signed accumulator overflow clamps to the finite `i32` range.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 4-, 2-, and 2-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must encode two distinct 2-bit indices; `0x0`, `0x5`, `0xa`, and `0xf` are undefined behavior.
/// `selector` must be the compile-time constant `0` or `1`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_m16n8k32_s32_s8_satfinite(
    c: [i32; 4],
    a: [u32; 2],
    b: [u32; 2],
    metadata: u32,
    selector: u32,
) -> [i32; 4] {
    let _ = (c, a, b, metadata, selector);
    unreachable!("mma_sp_m16n8k32_s32_s8_satfinite called outside CUDA kernel context")
}

/// Multiplies warp-distributed sparse signed A and unsigned B INT8 fragments and adds a wrapping s32 accumulator.
/// Lowers to `mma.sp.sync.aligned.m16n8k32.row.col.s32.s8.u8.s32`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 7.1 and `sm_80+`.
/// Signed accumulator overflow wraps.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 4-, 2-, and 2-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must encode two distinct 2-bit indices; `0x0`, `0x5`, `0xa`, and `0xf` are undefined behavior.
/// `selector` must be the compile-time constant `0` or `1`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_m16n8k32_s32_s8_u8(
    c: [i32; 4],
    a: [u32; 2],
    b: [u32; 2],
    metadata: u32,
    selector: u32,
) -> [i32; 4] {
    let _ = (c, a, b, metadata, selector);
    unreachable!("mma_sp_m16n8k32_s32_s8_u8 called outside CUDA kernel context")
}

/// Multiplies warp-distributed sparse signed A and unsigned B INT8 fragments and adds a saturating s32 accumulator.
/// Lowers to `mma.sp.sync.aligned.m16n8k32.row.col.satfinite.s32.s8.u8.s32`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 7.1 and `sm_80+`.
/// Signed accumulator overflow clamps to the finite `i32` range.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 4-, 2-, and 2-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must encode two distinct 2-bit indices; `0x0`, `0x5`, `0xa`, and `0xf` are undefined behavior.
/// `selector` must be the compile-time constant `0` or `1`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_m16n8k32_s32_s8_u8_satfinite(
    c: [i32; 4],
    a: [u32; 2],
    b: [u32; 2],
    metadata: u32,
    selector: u32,
) -> [i32; 4] {
    let _ = (c, a, b, metadata, selector);
    unreachable!("mma_sp_m16n8k32_s32_s8_u8_satfinite called outside CUDA kernel context")
}

/// Multiplies warp-distributed sparse unsigned A and unsigned B INT8 fragments and adds a wrapping s32 accumulator.
/// Lowers to `mma.sp.sync.aligned.m16n8k32.row.col.s32.u8.u8.s32`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 7.1 and `sm_80+`.
/// Signed accumulator overflow wraps.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 4-, 2-, and 2-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must encode two distinct 2-bit indices; `0x0`, `0x5`, `0xa`, and `0xf` are undefined behavior.
/// `selector` must be the compile-time constant `0` or `1`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_m16n8k32_s32_u8(
    c: [i32; 4],
    a: [u32; 2],
    b: [u32; 2],
    metadata: u32,
    selector: u32,
) -> [i32; 4] {
    let _ = (c, a, b, metadata, selector);
    unreachable!("mma_sp_m16n8k32_s32_u8 called outside CUDA kernel context")
}

/// Multiplies warp-distributed sparse unsigned A and signed B INT8 fragments and adds a wrapping s32 accumulator.
/// Lowers to `mma.sp.sync.aligned.m16n8k32.row.col.s32.u8.s8.s32`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 7.1 and `sm_80+`.
/// Signed accumulator overflow wraps.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 4-, 2-, and 2-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must encode two distinct 2-bit indices; `0x0`, `0x5`, `0xa`, and `0xf` are undefined behavior.
/// `selector` must be the compile-time constant `0` or `1`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_m16n8k32_s32_u8_s8(
    c: [i32; 4],
    a: [u32; 2],
    b: [u32; 2],
    metadata: u32,
    selector: u32,
) -> [i32; 4] {
    let _ = (c, a, b, metadata, selector);
    unreachable!("mma_sp_m16n8k32_s32_u8_s8 called outside CUDA kernel context")
}

/// Multiplies warp-distributed sparse unsigned A and signed B INT8 fragments and adds a saturating s32 accumulator.
/// Lowers to `mma.sp.sync.aligned.m16n8k32.row.col.satfinite.s32.u8.s8.s32`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 7.1 and `sm_80+`.
/// Signed accumulator overflow clamps to the finite `i32` range.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 4-, 2-, and 2-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must encode two distinct 2-bit indices; `0x0`, `0x5`, `0xa`, and `0xf` are undefined behavior.
/// `selector` must be the compile-time constant `0` or `1`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_m16n8k32_s32_u8_s8_satfinite(
    c: [i32; 4],
    a: [u32; 2],
    b: [u32; 2],
    metadata: u32,
    selector: u32,
) -> [i32; 4] {
    let _ = (c, a, b, metadata, selector);
    unreachable!("mma_sp_m16n8k32_s32_u8_s8_satfinite called outside CUDA kernel context")
}

/// Multiplies warp-distributed sparse unsigned A and unsigned B INT8 fragments and adds a saturating s32 accumulator.
/// Lowers to `mma.sp.sync.aligned.m16n8k32.row.col.satfinite.s32.u8.u8.s32`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 7.1 and `sm_80+`.
/// Signed accumulator overflow clamps to the finite `i32` range.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 4-, 2-, and 2-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must encode two distinct 2-bit indices; `0x0`, `0x5`, `0xa`, and `0xf` are undefined behavior.
/// `selector` must be the compile-time constant `0` or `1`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_m16n8k32_s32_u8_satfinite(
    c: [i32; 4],
    a: [u32; 2],
    b: [u32; 2],
    metadata: u32,
    selector: u32,
) -> [i32; 4] {
    let _ = (c, a, b, metadata, selector);
    unreachable!("mma_sp_m16n8k32_s32_u8_satfinite called outside CUDA kernel context")
}

/// Multiplies warp-distributed sparse signed A and signed B INT4 fragments and adds a wrapping s32 accumulator.
/// Lowers to `mma.sp.sync.aligned.m16n8k64.row.col.s32.s4.s4.s32`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 7.1 and `sm_80+`.
/// Signed accumulator overflow wraps.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 4-, 2-, and 2-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must encode two distinct 2-bit indices; `0x0`, `0x5`, `0xa`, and `0xf` are undefined behavior.
/// `selector` must be the compile-time constant `0` or `1`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_m16n8k64_s32_s4(
    c: [i32; 4],
    a: [u32; 2],
    b: [u32; 2],
    metadata: u32,
    selector: u32,
) -> [i32; 4] {
    let _ = (c, a, b, metadata, selector);
    unreachable!("mma_sp_m16n8k64_s32_s4 called outside CUDA kernel context")
}

/// Multiplies warp-distributed sparse signed A and signed B INT4 fragments and adds a saturating s32 accumulator.
/// Lowers to `mma.sp.sync.aligned.m16n8k64.row.col.satfinite.s32.s4.s4.s32`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 7.1 and `sm_80+`.
/// Signed accumulator overflow clamps to the finite `i32` range.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 4-, 2-, and 2-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must encode two distinct 2-bit indices; `0x0`, `0x5`, `0xa`, and `0xf` are undefined behavior.
/// `selector` must be the compile-time constant `0` or `1`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_m16n8k64_s32_s4_satfinite(
    c: [i32; 4],
    a: [u32; 2],
    b: [u32; 2],
    metadata: u32,
    selector: u32,
) -> [i32; 4] {
    let _ = (c, a, b, metadata, selector);
    unreachable!("mma_sp_m16n8k64_s32_s4_satfinite called outside CUDA kernel context")
}

/// Multiplies warp-distributed sparse signed A and unsigned B INT4 fragments and adds a wrapping s32 accumulator.
/// Lowers to `mma.sp.sync.aligned.m16n8k64.row.col.s32.s4.u4.s32`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 7.1 and `sm_80+`.
/// Signed accumulator overflow wraps.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 4-, 2-, and 2-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must encode two distinct 2-bit indices; `0x0`, `0x5`, `0xa`, and `0xf` are undefined behavior.
/// `selector` must be the compile-time constant `0` or `1`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_m16n8k64_s32_s4_u4(
    c: [i32; 4],
    a: [u32; 2],
    b: [u32; 2],
    metadata: u32,
    selector: u32,
) -> [i32; 4] {
    let _ = (c, a, b, metadata, selector);
    unreachable!("mma_sp_m16n8k64_s32_s4_u4 called outside CUDA kernel context")
}

/// Multiplies warp-distributed sparse signed A and unsigned B INT4 fragments and adds a saturating s32 accumulator.
/// Lowers to `mma.sp.sync.aligned.m16n8k64.row.col.satfinite.s32.s4.u4.s32`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 7.1 and `sm_80+`.
/// Signed accumulator overflow clamps to the finite `i32` range.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 4-, 2-, and 2-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must encode two distinct 2-bit indices; `0x0`, `0x5`, `0xa`, and `0xf` are undefined behavior.
/// `selector` must be the compile-time constant `0` or `1`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_m16n8k64_s32_s4_u4_satfinite(
    c: [i32; 4],
    a: [u32; 2],
    b: [u32; 2],
    metadata: u32,
    selector: u32,
) -> [i32; 4] {
    let _ = (c, a, b, metadata, selector);
    unreachable!("mma_sp_m16n8k64_s32_s4_u4_satfinite called outside CUDA kernel context")
}

/// Multiplies warp-distributed sparse signed A and signed B INT8 fragments and adds a wrapping s32 accumulator.
/// Lowers to `mma.sp.sync.aligned.m16n8k64.row.col.s32.s8.s8.s32`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 7.1 and `sm_80+`.
/// Signed accumulator overflow wraps.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 4-, 4-, and 4-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must encode two distinct 2-bit indices; `0x0`, `0x5`, `0xa`, and `0xf` are undefined behavior.
/// `selector` must be the compile-time constant `0`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_m16n8k64_s32_s8(
    c: [i32; 4],
    a: [u32; 4],
    b: [u32; 4],
    metadata: u32,
    selector: u32,
) -> [i32; 4] {
    let _ = (c, a, b, metadata, selector);
    unreachable!("mma_sp_m16n8k64_s32_s8 called outside CUDA kernel context")
}

/// Multiplies warp-distributed sparse signed A and signed B INT8 fragments and adds a saturating s32 accumulator.
/// Lowers to `mma.sp.sync.aligned.m16n8k64.row.col.satfinite.s32.s8.s8.s32`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 7.1 and `sm_80+`.
/// Signed accumulator overflow clamps to the finite `i32` range.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 4-, 4-, and 4-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must encode two distinct 2-bit indices; `0x0`, `0x5`, `0xa`, and `0xf` are undefined behavior.
/// `selector` must be the compile-time constant `0`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_m16n8k64_s32_s8_satfinite(
    c: [i32; 4],
    a: [u32; 4],
    b: [u32; 4],
    metadata: u32,
    selector: u32,
) -> [i32; 4] {
    let _ = (c, a, b, metadata, selector);
    unreachable!("mma_sp_m16n8k64_s32_s8_satfinite called outside CUDA kernel context")
}

/// Multiplies warp-distributed sparse signed A and unsigned B INT8 fragments and adds a wrapping s32 accumulator.
/// Lowers to `mma.sp.sync.aligned.m16n8k64.row.col.s32.s8.u8.s32`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 7.1 and `sm_80+`.
/// Signed accumulator overflow wraps.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 4-, 4-, and 4-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must encode two distinct 2-bit indices; `0x0`, `0x5`, `0xa`, and `0xf` are undefined behavior.
/// `selector` must be the compile-time constant `0`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_m16n8k64_s32_s8_u8(
    c: [i32; 4],
    a: [u32; 4],
    b: [u32; 4],
    metadata: u32,
    selector: u32,
) -> [i32; 4] {
    let _ = (c, a, b, metadata, selector);
    unreachable!("mma_sp_m16n8k64_s32_s8_u8 called outside CUDA kernel context")
}

/// Multiplies warp-distributed sparse signed A and unsigned B INT8 fragments and adds a saturating s32 accumulator.
/// Lowers to `mma.sp.sync.aligned.m16n8k64.row.col.satfinite.s32.s8.u8.s32`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 7.1 and `sm_80+`.
/// Signed accumulator overflow clamps to the finite `i32` range.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 4-, 4-, and 4-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must encode two distinct 2-bit indices; `0x0`, `0x5`, `0xa`, and `0xf` are undefined behavior.
/// `selector` must be the compile-time constant `0`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_m16n8k64_s32_s8_u8_satfinite(
    c: [i32; 4],
    a: [u32; 4],
    b: [u32; 4],
    metadata: u32,
    selector: u32,
) -> [i32; 4] {
    let _ = (c, a, b, metadata, selector);
    unreachable!("mma_sp_m16n8k64_s32_s8_u8_satfinite called outside CUDA kernel context")
}

/// Multiplies warp-distributed sparse unsigned A and unsigned B INT4 fragments and adds a wrapping s32 accumulator.
/// Lowers to `mma.sp.sync.aligned.m16n8k64.row.col.s32.u4.u4.s32`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 7.1 and `sm_80+`.
/// Signed accumulator overflow wraps.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 4-, 2-, and 2-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must encode two distinct 2-bit indices; `0x0`, `0x5`, `0xa`, and `0xf` are undefined behavior.
/// `selector` must be the compile-time constant `0` or `1`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_m16n8k64_s32_u4(
    c: [i32; 4],
    a: [u32; 2],
    b: [u32; 2],
    metadata: u32,
    selector: u32,
) -> [i32; 4] {
    let _ = (c, a, b, metadata, selector);
    unreachable!("mma_sp_m16n8k64_s32_u4 called outside CUDA kernel context")
}

/// Multiplies warp-distributed sparse unsigned A and signed B INT4 fragments and adds a wrapping s32 accumulator.
/// Lowers to `mma.sp.sync.aligned.m16n8k64.row.col.s32.u4.s4.s32`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 7.1 and `sm_80+`.
/// Signed accumulator overflow wraps.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 4-, 2-, and 2-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must encode two distinct 2-bit indices; `0x0`, `0x5`, `0xa`, and `0xf` are undefined behavior.
/// `selector` must be the compile-time constant `0` or `1`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_m16n8k64_s32_u4_s4(
    c: [i32; 4],
    a: [u32; 2],
    b: [u32; 2],
    metadata: u32,
    selector: u32,
) -> [i32; 4] {
    let _ = (c, a, b, metadata, selector);
    unreachable!("mma_sp_m16n8k64_s32_u4_s4 called outside CUDA kernel context")
}

/// Multiplies warp-distributed sparse unsigned A and signed B INT4 fragments and adds a saturating s32 accumulator.
/// Lowers to `mma.sp.sync.aligned.m16n8k64.row.col.satfinite.s32.u4.s4.s32`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 7.1 and `sm_80+`.
/// Signed accumulator overflow clamps to the finite `i32` range.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 4-, 2-, and 2-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must encode two distinct 2-bit indices; `0x0`, `0x5`, `0xa`, and `0xf` are undefined behavior.
/// `selector` must be the compile-time constant `0` or `1`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_m16n8k64_s32_u4_s4_satfinite(
    c: [i32; 4],
    a: [u32; 2],
    b: [u32; 2],
    metadata: u32,
    selector: u32,
) -> [i32; 4] {
    let _ = (c, a, b, metadata, selector);
    unreachable!("mma_sp_m16n8k64_s32_u4_s4_satfinite called outside CUDA kernel context")
}

/// Multiplies warp-distributed sparse unsigned A and unsigned B INT4 fragments and adds a saturating s32 accumulator.
/// Lowers to `mma.sp.sync.aligned.m16n8k64.row.col.satfinite.s32.u4.u4.s32`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 7.1 and `sm_80+`.
/// Signed accumulator overflow clamps to the finite `i32` range.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 4-, 2-, and 2-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must encode two distinct 2-bit indices; `0x0`, `0x5`, `0xa`, and `0xf` are undefined behavior.
/// `selector` must be the compile-time constant `0` or `1`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_m16n8k64_s32_u4_satfinite(
    c: [i32; 4],
    a: [u32; 2],
    b: [u32; 2],
    metadata: u32,
    selector: u32,
) -> [i32; 4] {
    let _ = (c, a, b, metadata, selector);
    unreachable!("mma_sp_m16n8k64_s32_u4_satfinite called outside CUDA kernel context")
}

/// Multiplies warp-distributed sparse unsigned A and unsigned B INT8 fragments and adds a wrapping s32 accumulator.
/// Lowers to `mma.sp.sync.aligned.m16n8k64.row.col.s32.u8.u8.s32`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 7.1 and `sm_80+`.
/// Signed accumulator overflow wraps.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 4-, 4-, and 4-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must encode two distinct 2-bit indices; `0x0`, `0x5`, `0xa`, and `0xf` are undefined behavior.
/// `selector` must be the compile-time constant `0`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_m16n8k64_s32_u8(
    c: [i32; 4],
    a: [u32; 4],
    b: [u32; 4],
    metadata: u32,
    selector: u32,
) -> [i32; 4] {
    let _ = (c, a, b, metadata, selector);
    unreachable!("mma_sp_m16n8k64_s32_u8 called outside CUDA kernel context")
}

/// Multiplies warp-distributed sparse unsigned A and signed B INT8 fragments and adds a wrapping s32 accumulator.
/// Lowers to `mma.sp.sync.aligned.m16n8k64.row.col.s32.u8.s8.s32`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 7.1 and `sm_80+`.
/// Signed accumulator overflow wraps.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 4-, 4-, and 4-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must encode two distinct 2-bit indices; `0x0`, `0x5`, `0xa`, and `0xf` are undefined behavior.
/// `selector` must be the compile-time constant `0`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_m16n8k64_s32_u8_s8(
    c: [i32; 4],
    a: [u32; 4],
    b: [u32; 4],
    metadata: u32,
    selector: u32,
) -> [i32; 4] {
    let _ = (c, a, b, metadata, selector);
    unreachable!("mma_sp_m16n8k64_s32_u8_s8 called outside CUDA kernel context")
}

/// Multiplies warp-distributed sparse unsigned A and signed B INT8 fragments and adds a saturating s32 accumulator.
/// Lowers to `mma.sp.sync.aligned.m16n8k64.row.col.satfinite.s32.u8.s8.s32`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 7.1 and `sm_80+`.
/// Signed accumulator overflow clamps to the finite `i32` range.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 4-, 4-, and 4-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must encode two distinct 2-bit indices; `0x0`, `0x5`, `0xa`, and `0xf` are undefined behavior.
/// `selector` must be the compile-time constant `0`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_m16n8k64_s32_u8_s8_satfinite(
    c: [i32; 4],
    a: [u32; 4],
    b: [u32; 4],
    metadata: u32,
    selector: u32,
) -> [i32; 4] {
    let _ = (c, a, b, metadata, selector);
    unreachable!("mma_sp_m16n8k64_s32_u8_s8_satfinite called outside CUDA kernel context")
}

/// Multiplies warp-distributed sparse unsigned A and unsigned B INT8 fragments and adds a saturating s32 accumulator.
/// Lowers to `mma.sp.sync.aligned.m16n8k64.row.col.satfinite.s32.u8.u8.s32`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 7.1 and `sm_80+`.
/// Signed accumulator overflow clamps to the finite `i32` range.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 4-, 4-, and 4-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must encode two distinct 2-bit indices; `0x0`, `0x5`, `0xa`, and `0xf` are undefined behavior.
/// `selector` must be the compile-time constant `0`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_m16n8k64_s32_u8_satfinite(
    c: [i32; 4],
    a: [u32; 4],
    b: [u32; 4],
    metadata: u32,
    selector: u32,
) -> [i32; 4] {
    let _ = (c, a, b, metadata, selector);
    unreachable!("mma_sp_m16n8k64_s32_u8_satfinite called outside CUDA kernel context")
}

/// Multiplies warp-distributed sparse signed A and signed B INT4 fragments with ordered sparsity metadata and adds a wrapping s32 accumulator.
/// Lowers to `mma.sp::ordered_metadata.sync.aligned.m16n8k128.row.col.s32.s4.s4.s32`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 8.5 and `sm_80+`.
/// Signed accumulator overflow wraps.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 4-, 4-, and 4-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must be `0x4`, `0x8`, `0x9`, `0xc`, `0xd`, or `0xe`; any other value is undefined behavior.
/// `selector` must be the compile-time constant `0`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_ordered_metadata_m16n8k128_s32_s4(
    c: [i32; 4],
    a: [u32; 4],
    b: [u32; 4],
    metadata: u32,
    selector: u32,
) -> [i32; 4] {
    let _ = (c, a, b, metadata, selector);
    unreachable!("mma_sp_ordered_metadata_m16n8k128_s32_s4 called outside CUDA kernel context")
}

/// Multiplies warp-distributed sparse signed A and signed B INT4 fragments with ordered sparsity metadata and adds a saturating s32 accumulator.
/// Lowers to `mma.sp::ordered_metadata.sync.aligned.m16n8k128.row.col.satfinite.s32.s4.s4.s32`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 8.5 and `sm_80+`.
/// Signed accumulator overflow clamps to the finite `i32` range.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 4-, 4-, and 4-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must be `0x4`, `0x8`, `0x9`, `0xc`, `0xd`, or `0xe`; any other value is undefined behavior.
/// `selector` must be the compile-time constant `0`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_ordered_metadata_m16n8k128_s32_s4_satfinite(
    c: [i32; 4],
    a: [u32; 4],
    b: [u32; 4],
    metadata: u32,
    selector: u32,
) -> [i32; 4] {
    let _ = (c, a, b, metadata, selector);
    unreachable!(
        "mma_sp_ordered_metadata_m16n8k128_s32_s4_satfinite called outside CUDA kernel context"
    )
}

/// Multiplies warp-distributed sparse signed A and unsigned B INT4 fragments with ordered sparsity metadata and adds a wrapping s32 accumulator.
/// Lowers to `mma.sp::ordered_metadata.sync.aligned.m16n8k128.row.col.s32.s4.u4.s32`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 8.5 and `sm_80+`.
/// Signed accumulator overflow wraps.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 4-, 4-, and 4-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must be `0x4`, `0x8`, `0x9`, `0xc`, `0xd`, or `0xe`; any other value is undefined behavior.
/// `selector` must be the compile-time constant `0`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_ordered_metadata_m16n8k128_s32_s4_u4(
    c: [i32; 4],
    a: [u32; 4],
    b: [u32; 4],
    metadata: u32,
    selector: u32,
) -> [i32; 4] {
    let _ = (c, a, b, metadata, selector);
    unreachable!("mma_sp_ordered_metadata_m16n8k128_s32_s4_u4 called outside CUDA kernel context")
}

/// Multiplies warp-distributed sparse signed A and unsigned B INT4 fragments with ordered sparsity metadata and adds a saturating s32 accumulator.
/// Lowers to `mma.sp::ordered_metadata.sync.aligned.m16n8k128.row.col.satfinite.s32.s4.u4.s32`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 8.5 and `sm_80+`.
/// Signed accumulator overflow clamps to the finite `i32` range.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 4-, 4-, and 4-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must be `0x4`, `0x8`, `0x9`, `0xc`, `0xd`, or `0xe`; any other value is undefined behavior.
/// `selector` must be the compile-time constant `0`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_ordered_metadata_m16n8k128_s32_s4_u4_satfinite(
    c: [i32; 4],
    a: [u32; 4],
    b: [u32; 4],
    metadata: u32,
    selector: u32,
) -> [i32; 4] {
    let _ = (c, a, b, metadata, selector);
    unreachable!(
        "mma_sp_ordered_metadata_m16n8k128_s32_s4_u4_satfinite called outside CUDA kernel context"
    )
}

/// Multiplies warp-distributed sparse unsigned A and unsigned B INT4 fragments with ordered sparsity metadata and adds a wrapping s32 accumulator.
/// Lowers to `mma.sp::ordered_metadata.sync.aligned.m16n8k128.row.col.s32.u4.u4.s32`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 8.5 and `sm_80+`.
/// Signed accumulator overflow wraps.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 4-, 4-, and 4-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must be `0x4`, `0x8`, `0x9`, `0xc`, `0xd`, or `0xe`; any other value is undefined behavior.
/// `selector` must be the compile-time constant `0`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_ordered_metadata_m16n8k128_s32_u4(
    c: [i32; 4],
    a: [u32; 4],
    b: [u32; 4],
    metadata: u32,
    selector: u32,
) -> [i32; 4] {
    let _ = (c, a, b, metadata, selector);
    unreachable!("mma_sp_ordered_metadata_m16n8k128_s32_u4 called outside CUDA kernel context")
}

/// Multiplies warp-distributed sparse unsigned A and signed B INT4 fragments with ordered sparsity metadata and adds a wrapping s32 accumulator.
/// Lowers to `mma.sp::ordered_metadata.sync.aligned.m16n8k128.row.col.s32.u4.s4.s32`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 8.5 and `sm_80+`.
/// Signed accumulator overflow wraps.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 4-, 4-, and 4-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must be `0x4`, `0x8`, `0x9`, `0xc`, `0xd`, or `0xe`; any other value is undefined behavior.
/// `selector` must be the compile-time constant `0`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_ordered_metadata_m16n8k128_s32_u4_s4(
    c: [i32; 4],
    a: [u32; 4],
    b: [u32; 4],
    metadata: u32,
    selector: u32,
) -> [i32; 4] {
    let _ = (c, a, b, metadata, selector);
    unreachable!("mma_sp_ordered_metadata_m16n8k128_s32_u4_s4 called outside CUDA kernel context")
}

/// Multiplies warp-distributed sparse unsigned A and signed B INT4 fragments with ordered sparsity metadata and adds a saturating s32 accumulator.
/// Lowers to `mma.sp::ordered_metadata.sync.aligned.m16n8k128.row.col.satfinite.s32.u4.s4.s32`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 8.5 and `sm_80+`.
/// Signed accumulator overflow clamps to the finite `i32` range.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 4-, 4-, and 4-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must be `0x4`, `0x8`, `0x9`, `0xc`, `0xd`, or `0xe`; any other value is undefined behavior.
/// `selector` must be the compile-time constant `0`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_ordered_metadata_m16n8k128_s32_u4_s4_satfinite(
    c: [i32; 4],
    a: [u32; 4],
    b: [u32; 4],
    metadata: u32,
    selector: u32,
) -> [i32; 4] {
    let _ = (c, a, b, metadata, selector);
    unreachable!(
        "mma_sp_ordered_metadata_m16n8k128_s32_u4_s4_satfinite called outside CUDA kernel context"
    )
}

/// Multiplies warp-distributed sparse unsigned A and unsigned B INT4 fragments with ordered sparsity metadata and adds a saturating s32 accumulator.
/// Lowers to `mma.sp::ordered_metadata.sync.aligned.m16n8k128.row.col.satfinite.s32.u4.u4.s32`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 8.5 and `sm_80+`.
/// Signed accumulator overflow clamps to the finite `i32` range.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 4-, 4-, and 4-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must be `0x4`, `0x8`, `0x9`, `0xc`, `0xd`, or `0xe`; any other value is undefined behavior.
/// `selector` must be the compile-time constant `0`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_ordered_metadata_m16n8k128_s32_u4_satfinite(
    c: [i32; 4],
    a: [u32; 4],
    b: [u32; 4],
    metadata: u32,
    selector: u32,
) -> [i32; 4] {
    let _ = (c, a, b, metadata, selector);
    unreachable!(
        "mma_sp_ordered_metadata_m16n8k128_s32_u4_satfinite called outside CUDA kernel context"
    )
}

/// Multiplies warp-distributed sparse signed A and signed B INT8 fragments with ordered sparsity metadata and adds a wrapping s32 accumulator.
/// Lowers to `mma.sp::ordered_metadata.sync.aligned.m16n8k32.row.col.s32.s8.s8.s32`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 8.5 and `sm_80+`.
/// Signed accumulator overflow wraps.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 4-, 2-, and 2-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must be `0x4`, `0x8`, `0x9`, `0xc`, `0xd`, or `0xe`; any other value is undefined behavior.
/// `selector` must be the compile-time constant `0` or `1`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_ordered_metadata_m16n8k32_s32_s8(
    c: [i32; 4],
    a: [u32; 2],
    b: [u32; 2],
    metadata: u32,
    selector: u32,
) -> [i32; 4] {
    let _ = (c, a, b, metadata, selector);
    unreachable!("mma_sp_ordered_metadata_m16n8k32_s32_s8 called outside CUDA kernel context")
}

/// Multiplies warp-distributed sparse signed A and signed B INT8 fragments with ordered sparsity metadata and adds a saturating s32 accumulator.
/// Lowers to `mma.sp::ordered_metadata.sync.aligned.m16n8k32.row.col.satfinite.s32.s8.s8.s32`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 8.5 and `sm_80+`.
/// Signed accumulator overflow clamps to the finite `i32` range.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 4-, 2-, and 2-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must be `0x4`, `0x8`, `0x9`, `0xc`, `0xd`, or `0xe`; any other value is undefined behavior.
/// `selector` must be the compile-time constant `0` or `1`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_ordered_metadata_m16n8k32_s32_s8_satfinite(
    c: [i32; 4],
    a: [u32; 2],
    b: [u32; 2],
    metadata: u32,
    selector: u32,
) -> [i32; 4] {
    let _ = (c, a, b, metadata, selector);
    unreachable!(
        "mma_sp_ordered_metadata_m16n8k32_s32_s8_satfinite called outside CUDA kernel context"
    )
}

/// Multiplies warp-distributed sparse signed A and unsigned B INT8 fragments with ordered sparsity metadata and adds a wrapping s32 accumulator.
/// Lowers to `mma.sp::ordered_metadata.sync.aligned.m16n8k32.row.col.s32.s8.u8.s32`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 8.5 and `sm_80+`.
/// Signed accumulator overflow wraps.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 4-, 2-, and 2-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must be `0x4`, `0x8`, `0x9`, `0xc`, `0xd`, or `0xe`; any other value is undefined behavior.
/// `selector` must be the compile-time constant `0` or `1`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_ordered_metadata_m16n8k32_s32_s8_u8(
    c: [i32; 4],
    a: [u32; 2],
    b: [u32; 2],
    metadata: u32,
    selector: u32,
) -> [i32; 4] {
    let _ = (c, a, b, metadata, selector);
    unreachable!("mma_sp_ordered_metadata_m16n8k32_s32_s8_u8 called outside CUDA kernel context")
}

/// Multiplies warp-distributed sparse signed A and unsigned B INT8 fragments with ordered sparsity metadata and adds a saturating s32 accumulator.
/// Lowers to `mma.sp::ordered_metadata.sync.aligned.m16n8k32.row.col.satfinite.s32.s8.u8.s32`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 8.5 and `sm_80+`.
/// Signed accumulator overflow clamps to the finite `i32` range.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 4-, 2-, and 2-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must be `0x4`, `0x8`, `0x9`, `0xc`, `0xd`, or `0xe`; any other value is undefined behavior.
/// `selector` must be the compile-time constant `0` or `1`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_ordered_metadata_m16n8k32_s32_s8_u8_satfinite(
    c: [i32; 4],
    a: [u32; 2],
    b: [u32; 2],
    metadata: u32,
    selector: u32,
) -> [i32; 4] {
    let _ = (c, a, b, metadata, selector);
    unreachable!(
        "mma_sp_ordered_metadata_m16n8k32_s32_s8_u8_satfinite called outside CUDA kernel context"
    )
}

/// Multiplies warp-distributed sparse unsigned A and unsigned B INT8 fragments with ordered sparsity metadata and adds a wrapping s32 accumulator.
/// Lowers to `mma.sp::ordered_metadata.sync.aligned.m16n8k32.row.col.s32.u8.u8.s32`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 8.5 and `sm_80+`.
/// Signed accumulator overflow wraps.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 4-, 2-, and 2-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must be `0x4`, `0x8`, `0x9`, `0xc`, `0xd`, or `0xe`; any other value is undefined behavior.
/// `selector` must be the compile-time constant `0` or `1`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_ordered_metadata_m16n8k32_s32_u8(
    c: [i32; 4],
    a: [u32; 2],
    b: [u32; 2],
    metadata: u32,
    selector: u32,
) -> [i32; 4] {
    let _ = (c, a, b, metadata, selector);
    unreachable!("mma_sp_ordered_metadata_m16n8k32_s32_u8 called outside CUDA kernel context")
}

/// Multiplies warp-distributed sparse unsigned A and signed B INT8 fragments with ordered sparsity metadata and adds a wrapping s32 accumulator.
/// Lowers to `mma.sp::ordered_metadata.sync.aligned.m16n8k32.row.col.s32.u8.s8.s32`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 8.5 and `sm_80+`.
/// Signed accumulator overflow wraps.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 4-, 2-, and 2-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must be `0x4`, `0x8`, `0x9`, `0xc`, `0xd`, or `0xe`; any other value is undefined behavior.
/// `selector` must be the compile-time constant `0` or `1`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_ordered_metadata_m16n8k32_s32_u8_s8(
    c: [i32; 4],
    a: [u32; 2],
    b: [u32; 2],
    metadata: u32,
    selector: u32,
) -> [i32; 4] {
    let _ = (c, a, b, metadata, selector);
    unreachable!("mma_sp_ordered_metadata_m16n8k32_s32_u8_s8 called outside CUDA kernel context")
}

/// Multiplies warp-distributed sparse unsigned A and signed B INT8 fragments with ordered sparsity metadata and adds a saturating s32 accumulator.
/// Lowers to `mma.sp::ordered_metadata.sync.aligned.m16n8k32.row.col.satfinite.s32.u8.s8.s32`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 8.5 and `sm_80+`.
/// Signed accumulator overflow clamps to the finite `i32` range.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 4-, 2-, and 2-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must be `0x4`, `0x8`, `0x9`, `0xc`, `0xd`, or `0xe`; any other value is undefined behavior.
/// `selector` must be the compile-time constant `0` or `1`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_ordered_metadata_m16n8k32_s32_u8_s8_satfinite(
    c: [i32; 4],
    a: [u32; 2],
    b: [u32; 2],
    metadata: u32,
    selector: u32,
) -> [i32; 4] {
    let _ = (c, a, b, metadata, selector);
    unreachable!(
        "mma_sp_ordered_metadata_m16n8k32_s32_u8_s8_satfinite called outside CUDA kernel context"
    )
}

/// Multiplies warp-distributed sparse unsigned A and unsigned B INT8 fragments with ordered sparsity metadata and adds a saturating s32 accumulator.
/// Lowers to `mma.sp::ordered_metadata.sync.aligned.m16n8k32.row.col.satfinite.s32.u8.u8.s32`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 8.5 and `sm_80+`.
/// Signed accumulator overflow clamps to the finite `i32` range.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 4-, 2-, and 2-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must be `0x4`, `0x8`, `0x9`, `0xc`, `0xd`, or `0xe`; any other value is undefined behavior.
/// `selector` must be the compile-time constant `0` or `1`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_ordered_metadata_m16n8k32_s32_u8_satfinite(
    c: [i32; 4],
    a: [u32; 2],
    b: [u32; 2],
    metadata: u32,
    selector: u32,
) -> [i32; 4] {
    let _ = (c, a, b, metadata, selector);
    unreachable!(
        "mma_sp_ordered_metadata_m16n8k32_s32_u8_satfinite called outside CUDA kernel context"
    )
}

/// Multiplies warp-distributed sparse e2m1 A and e2m1 B fragments and adds a packed F16 accumulator.
/// Lowers to `mma.sp::ordered_metadata.sync.aligned.m16n8k64.row.col.kind::f8f6f4.f16.e2m1.e2m1.f16`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 8.7 and `sm_120a or sm_120f or sm_121a or sm_121f`.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 2-, 4-, and 4-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must be `0x4`, `0x8`, `0x9`, `0xc`, `0xd`, or `0xe`; any other value is undefined behavior.
/// `selector` must be the compile-time constant `0`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f16_e2m1_e2m1_f16(
    c: [u32; 2],
    a: [u32; 4],
    b: [u32; 4],
    metadata: u32,
    selector: u32,
) -> [u32; 2] {
    let _ = (c, a, b, metadata, selector);
    unreachable!(
        "mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f16_e2m1_e2m1_f16 called outside CUDA kernel context"
    )
}

/// Multiplies warp-distributed sparse e2m1 A and e2m3 B fragments and adds a packed F16 accumulator.
/// Lowers to `mma.sp::ordered_metadata.sync.aligned.m16n8k64.row.col.kind::f8f6f4.f16.e2m1.e2m3.f16`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 8.7 and `sm_120a or sm_120f or sm_121a or sm_121f`.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 2-, 4-, and 4-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must be `0x4`, `0x8`, `0x9`, `0xc`, `0xd`, or `0xe`; any other value is undefined behavior.
/// `selector` must be the compile-time constant `0`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f16_e2m1_e2m3_f16(
    c: [u32; 2],
    a: [u32; 4],
    b: [u32; 4],
    metadata: u32,
    selector: u32,
) -> [u32; 2] {
    let _ = (c, a, b, metadata, selector);
    unreachable!(
        "mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f16_e2m1_e2m3_f16 called outside CUDA kernel context"
    )
}

/// Multiplies warp-distributed sparse e2m1 A and e3m2 B fragments and adds a packed F16 accumulator.
/// Lowers to `mma.sp::ordered_metadata.sync.aligned.m16n8k64.row.col.kind::f8f6f4.f16.e2m1.e3m2.f16`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 8.7 and `sm_120a or sm_120f or sm_121a or sm_121f`.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 2-, 4-, and 4-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must be `0x4`, `0x8`, `0x9`, `0xc`, `0xd`, or `0xe`; any other value is undefined behavior.
/// `selector` must be the compile-time constant `0`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f16_e2m1_e3m2_f16(
    c: [u32; 2],
    a: [u32; 4],
    b: [u32; 4],
    metadata: u32,
    selector: u32,
) -> [u32; 2] {
    let _ = (c, a, b, metadata, selector);
    unreachable!(
        "mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f16_e2m1_e3m2_f16 called outside CUDA kernel context"
    )
}

/// Multiplies warp-distributed sparse e2m1 A and e4m3 B fragments and adds a packed F16 accumulator.
/// Lowers to `mma.sp::ordered_metadata.sync.aligned.m16n8k64.row.col.kind::f8f6f4.f16.e2m1.e4m3.f16`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 8.7 and `sm_120a or sm_120f or sm_121a or sm_121f`.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 2-, 4-, and 4-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must be `0x4`, `0x8`, `0x9`, `0xc`, `0xd`, or `0xe`; any other value is undefined behavior.
/// `selector` must be the compile-time constant `0`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f16_e2m1_e4m3_f16(
    c: [u32; 2],
    a: [u32; 4],
    b: [u32; 4],
    metadata: u32,
    selector: u32,
) -> [u32; 2] {
    let _ = (c, a, b, metadata, selector);
    unreachable!(
        "mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f16_e2m1_e4m3_f16 called outside CUDA kernel context"
    )
}

/// Multiplies warp-distributed sparse e2m1 A and e5m2 B fragments and adds a packed F16 accumulator.
/// Lowers to `mma.sp::ordered_metadata.sync.aligned.m16n8k64.row.col.kind::f8f6f4.f16.e2m1.e5m2.f16`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 8.7 and `sm_120a or sm_120f or sm_121a or sm_121f`.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 2-, 4-, and 4-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must be `0x4`, `0x8`, `0x9`, `0xc`, `0xd`, or `0xe`; any other value is undefined behavior.
/// `selector` must be the compile-time constant `0`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f16_e2m1_e5m2_f16(
    c: [u32; 2],
    a: [u32; 4],
    b: [u32; 4],
    metadata: u32,
    selector: u32,
) -> [u32; 2] {
    let _ = (c, a, b, metadata, selector);
    unreachable!(
        "mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f16_e2m1_e5m2_f16 called outside CUDA kernel context"
    )
}

/// Multiplies warp-distributed sparse e2m3 A and e2m1 B fragments and adds a packed F16 accumulator.
/// Lowers to `mma.sp::ordered_metadata.sync.aligned.m16n8k64.row.col.kind::f8f6f4.f16.e2m3.e2m1.f16`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 8.7 and `sm_120a or sm_120f or sm_121a or sm_121f`.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 2-, 4-, and 4-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must be `0x4`, `0x8`, `0x9`, `0xc`, `0xd`, or `0xe`; any other value is undefined behavior.
/// `selector` must be the compile-time constant `0`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f16_e2m3_e2m1_f16(
    c: [u32; 2],
    a: [u32; 4],
    b: [u32; 4],
    metadata: u32,
    selector: u32,
) -> [u32; 2] {
    let _ = (c, a, b, metadata, selector);
    unreachable!(
        "mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f16_e2m3_e2m1_f16 called outside CUDA kernel context"
    )
}

/// Multiplies warp-distributed sparse e2m3 A and e2m3 B fragments and adds a packed F16 accumulator.
/// Lowers to `mma.sp::ordered_metadata.sync.aligned.m16n8k64.row.col.kind::f8f6f4.f16.e2m3.e2m3.f16`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 8.7 and `sm_120a or sm_120f or sm_121a or sm_121f`.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 2-, 4-, and 4-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must be `0x4`, `0x8`, `0x9`, `0xc`, `0xd`, or `0xe`; any other value is undefined behavior.
/// `selector` must be the compile-time constant `0`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f16_e2m3_e2m3_f16(
    c: [u32; 2],
    a: [u32; 4],
    b: [u32; 4],
    metadata: u32,
    selector: u32,
) -> [u32; 2] {
    let _ = (c, a, b, metadata, selector);
    unreachable!(
        "mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f16_e2m3_e2m3_f16 called outside CUDA kernel context"
    )
}

/// Multiplies warp-distributed sparse e2m3 A and e3m2 B fragments and adds a packed F16 accumulator.
/// Lowers to `mma.sp::ordered_metadata.sync.aligned.m16n8k64.row.col.kind::f8f6f4.f16.e2m3.e3m2.f16`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 8.7 and `sm_120a or sm_120f or sm_121a or sm_121f`.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 2-, 4-, and 4-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must be `0x4`, `0x8`, `0x9`, `0xc`, `0xd`, or `0xe`; any other value is undefined behavior.
/// `selector` must be the compile-time constant `0`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f16_e2m3_e3m2_f16(
    c: [u32; 2],
    a: [u32; 4],
    b: [u32; 4],
    metadata: u32,
    selector: u32,
) -> [u32; 2] {
    let _ = (c, a, b, metadata, selector);
    unreachable!(
        "mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f16_e2m3_e3m2_f16 called outside CUDA kernel context"
    )
}

/// Multiplies warp-distributed sparse e2m3 A and e4m3 B fragments and adds a packed F16 accumulator.
/// Lowers to `mma.sp::ordered_metadata.sync.aligned.m16n8k64.row.col.kind::f8f6f4.f16.e2m3.e4m3.f16`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 8.7 and `sm_120a or sm_120f or sm_121a or sm_121f`.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 2-, 4-, and 4-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must be `0x4`, `0x8`, `0x9`, `0xc`, `0xd`, or `0xe`; any other value is undefined behavior.
/// `selector` must be the compile-time constant `0`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f16_e2m3_e4m3_f16(
    c: [u32; 2],
    a: [u32; 4],
    b: [u32; 4],
    metadata: u32,
    selector: u32,
) -> [u32; 2] {
    let _ = (c, a, b, metadata, selector);
    unreachable!(
        "mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f16_e2m3_e4m3_f16 called outside CUDA kernel context"
    )
}

/// Multiplies warp-distributed sparse e2m3 A and e5m2 B fragments and adds a packed F16 accumulator.
/// Lowers to `mma.sp::ordered_metadata.sync.aligned.m16n8k64.row.col.kind::f8f6f4.f16.e2m3.e5m2.f16`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 8.7 and `sm_120a or sm_120f or sm_121a or sm_121f`.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 2-, 4-, and 4-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must be `0x4`, `0x8`, `0x9`, `0xc`, `0xd`, or `0xe`; any other value is undefined behavior.
/// `selector` must be the compile-time constant `0`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f16_e2m3_e5m2_f16(
    c: [u32; 2],
    a: [u32; 4],
    b: [u32; 4],
    metadata: u32,
    selector: u32,
) -> [u32; 2] {
    let _ = (c, a, b, metadata, selector);
    unreachable!(
        "mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f16_e2m3_e5m2_f16 called outside CUDA kernel context"
    )
}

/// Multiplies warp-distributed sparse e3m2 A and e2m1 B fragments and adds a packed F16 accumulator.
/// Lowers to `mma.sp::ordered_metadata.sync.aligned.m16n8k64.row.col.kind::f8f6f4.f16.e3m2.e2m1.f16`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 8.7 and `sm_120a or sm_120f or sm_121a or sm_121f`.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 2-, 4-, and 4-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must be `0x4`, `0x8`, `0x9`, `0xc`, `0xd`, or `0xe`; any other value is undefined behavior.
/// `selector` must be the compile-time constant `0`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f16_e3m2_e2m1_f16(
    c: [u32; 2],
    a: [u32; 4],
    b: [u32; 4],
    metadata: u32,
    selector: u32,
) -> [u32; 2] {
    let _ = (c, a, b, metadata, selector);
    unreachable!(
        "mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f16_e3m2_e2m1_f16 called outside CUDA kernel context"
    )
}

/// Multiplies warp-distributed sparse e3m2 A and e2m3 B fragments and adds a packed F16 accumulator.
/// Lowers to `mma.sp::ordered_metadata.sync.aligned.m16n8k64.row.col.kind::f8f6f4.f16.e3m2.e2m3.f16`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 8.7 and `sm_120a or sm_120f or sm_121a or sm_121f`.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 2-, 4-, and 4-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must be `0x4`, `0x8`, `0x9`, `0xc`, `0xd`, or `0xe`; any other value is undefined behavior.
/// `selector` must be the compile-time constant `0`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f16_e3m2_e2m3_f16(
    c: [u32; 2],
    a: [u32; 4],
    b: [u32; 4],
    metadata: u32,
    selector: u32,
) -> [u32; 2] {
    let _ = (c, a, b, metadata, selector);
    unreachable!(
        "mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f16_e3m2_e2m3_f16 called outside CUDA kernel context"
    )
}

/// Multiplies warp-distributed sparse e3m2 A and e3m2 B fragments and adds a packed F16 accumulator.
/// Lowers to `mma.sp::ordered_metadata.sync.aligned.m16n8k64.row.col.kind::f8f6f4.f16.e3m2.e3m2.f16`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 8.7 and `sm_120a or sm_120f or sm_121a or sm_121f`.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 2-, 4-, and 4-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must be `0x4`, `0x8`, `0x9`, `0xc`, `0xd`, or `0xe`; any other value is undefined behavior.
/// `selector` must be the compile-time constant `0`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f16_e3m2_e3m2_f16(
    c: [u32; 2],
    a: [u32; 4],
    b: [u32; 4],
    metadata: u32,
    selector: u32,
) -> [u32; 2] {
    let _ = (c, a, b, metadata, selector);
    unreachable!(
        "mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f16_e3m2_e3m2_f16 called outside CUDA kernel context"
    )
}

/// Multiplies warp-distributed sparse e3m2 A and e4m3 B fragments and adds a packed F16 accumulator.
/// Lowers to `mma.sp::ordered_metadata.sync.aligned.m16n8k64.row.col.kind::f8f6f4.f16.e3m2.e4m3.f16`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 8.7 and `sm_120a or sm_120f or sm_121a or sm_121f`.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 2-, 4-, and 4-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must be `0x4`, `0x8`, `0x9`, `0xc`, `0xd`, or `0xe`; any other value is undefined behavior.
/// `selector` must be the compile-time constant `0`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f16_e3m2_e4m3_f16(
    c: [u32; 2],
    a: [u32; 4],
    b: [u32; 4],
    metadata: u32,
    selector: u32,
) -> [u32; 2] {
    let _ = (c, a, b, metadata, selector);
    unreachable!(
        "mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f16_e3m2_e4m3_f16 called outside CUDA kernel context"
    )
}

/// Multiplies warp-distributed sparse e3m2 A and e5m2 B fragments and adds a packed F16 accumulator.
/// Lowers to `mma.sp::ordered_metadata.sync.aligned.m16n8k64.row.col.kind::f8f6f4.f16.e3m2.e5m2.f16`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 8.7 and `sm_120a or sm_120f or sm_121a or sm_121f`.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 2-, 4-, and 4-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must be `0x4`, `0x8`, `0x9`, `0xc`, `0xd`, or `0xe`; any other value is undefined behavior.
/// `selector` must be the compile-time constant `0`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f16_e3m2_e5m2_f16(
    c: [u32; 2],
    a: [u32; 4],
    b: [u32; 4],
    metadata: u32,
    selector: u32,
) -> [u32; 2] {
    let _ = (c, a, b, metadata, selector);
    unreachable!(
        "mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f16_e3m2_e5m2_f16 called outside CUDA kernel context"
    )
}

/// Multiplies warp-distributed sparse e4m3 A and e2m1 B fragments and adds a packed F16 accumulator.
/// Lowers to `mma.sp::ordered_metadata.sync.aligned.m16n8k64.row.col.kind::f8f6f4.f16.e4m3.e2m1.f16`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 8.7 and `sm_120a or sm_120f or sm_121a or sm_121f`.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 2-, 4-, and 4-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must be `0x4`, `0x8`, `0x9`, `0xc`, `0xd`, or `0xe`; any other value is undefined behavior.
/// `selector` must be the compile-time constant `0`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f16_e4m3_e2m1_f16(
    c: [u32; 2],
    a: [u32; 4],
    b: [u32; 4],
    metadata: u32,
    selector: u32,
) -> [u32; 2] {
    let _ = (c, a, b, metadata, selector);
    unreachable!(
        "mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f16_e4m3_e2m1_f16 called outside CUDA kernel context"
    )
}

/// Multiplies warp-distributed sparse e4m3 A and e2m3 B fragments and adds a packed F16 accumulator.
/// Lowers to `mma.sp::ordered_metadata.sync.aligned.m16n8k64.row.col.kind::f8f6f4.f16.e4m3.e2m3.f16`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 8.7 and `sm_120a or sm_120f or sm_121a or sm_121f`.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 2-, 4-, and 4-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must be `0x4`, `0x8`, `0x9`, `0xc`, `0xd`, or `0xe`; any other value is undefined behavior.
/// `selector` must be the compile-time constant `0`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f16_e4m3_e2m3_f16(
    c: [u32; 2],
    a: [u32; 4],
    b: [u32; 4],
    metadata: u32,
    selector: u32,
) -> [u32; 2] {
    let _ = (c, a, b, metadata, selector);
    unreachable!(
        "mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f16_e4m3_e2m3_f16 called outside CUDA kernel context"
    )
}

/// Multiplies warp-distributed sparse e4m3 A and e3m2 B fragments and adds a packed F16 accumulator.
/// Lowers to `mma.sp::ordered_metadata.sync.aligned.m16n8k64.row.col.kind::f8f6f4.f16.e4m3.e3m2.f16`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 8.7 and `sm_120a or sm_120f or sm_121a or sm_121f`.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 2-, 4-, and 4-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must be `0x4`, `0x8`, `0x9`, `0xc`, `0xd`, or `0xe`; any other value is undefined behavior.
/// `selector` must be the compile-time constant `0`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f16_e4m3_e3m2_f16(
    c: [u32; 2],
    a: [u32; 4],
    b: [u32; 4],
    metadata: u32,
    selector: u32,
) -> [u32; 2] {
    let _ = (c, a, b, metadata, selector);
    unreachable!(
        "mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f16_e4m3_e3m2_f16 called outside CUDA kernel context"
    )
}

/// Multiplies warp-distributed sparse e4m3 A and e4m3 B fragments and adds a packed F16 accumulator.
/// Lowers to `mma.sp::ordered_metadata.sync.aligned.m16n8k64.row.col.kind::f8f6f4.f16.e4m3.e4m3.f16`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 8.7 and `sm_120a or sm_120f or sm_121a or sm_121f`.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 2-, 4-, and 4-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must be `0x4`, `0x8`, `0x9`, `0xc`, `0xd`, or `0xe`; any other value is undefined behavior.
/// `selector` must be the compile-time constant `0`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f16_e4m3_e4m3_f16(
    c: [u32; 2],
    a: [u32; 4],
    b: [u32; 4],
    metadata: u32,
    selector: u32,
) -> [u32; 2] {
    let _ = (c, a, b, metadata, selector);
    unreachable!(
        "mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f16_e4m3_e4m3_f16 called outside CUDA kernel context"
    )
}

/// Multiplies warp-distributed sparse e4m3 A and e5m2 B fragments and adds a packed F16 accumulator.
/// Lowers to `mma.sp::ordered_metadata.sync.aligned.m16n8k64.row.col.kind::f8f6f4.f16.e4m3.e5m2.f16`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 8.7 and `sm_120a or sm_120f or sm_121a or sm_121f`.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 2-, 4-, and 4-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must be `0x4`, `0x8`, `0x9`, `0xc`, `0xd`, or `0xe`; any other value is undefined behavior.
/// `selector` must be the compile-time constant `0`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f16_e4m3_e5m2_f16(
    c: [u32; 2],
    a: [u32; 4],
    b: [u32; 4],
    metadata: u32,
    selector: u32,
) -> [u32; 2] {
    let _ = (c, a, b, metadata, selector);
    unreachable!(
        "mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f16_e4m3_e5m2_f16 called outside CUDA kernel context"
    )
}

/// Multiplies warp-distributed sparse e5m2 A and e2m1 B fragments and adds a packed F16 accumulator.
/// Lowers to `mma.sp::ordered_metadata.sync.aligned.m16n8k64.row.col.kind::f8f6f4.f16.e5m2.e2m1.f16`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 8.7 and `sm_120a or sm_120f or sm_121a or sm_121f`.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 2-, 4-, and 4-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must be `0x4`, `0x8`, `0x9`, `0xc`, `0xd`, or `0xe`; any other value is undefined behavior.
/// `selector` must be the compile-time constant `0`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f16_e5m2_e2m1_f16(
    c: [u32; 2],
    a: [u32; 4],
    b: [u32; 4],
    metadata: u32,
    selector: u32,
) -> [u32; 2] {
    let _ = (c, a, b, metadata, selector);
    unreachable!(
        "mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f16_e5m2_e2m1_f16 called outside CUDA kernel context"
    )
}

/// Multiplies warp-distributed sparse e5m2 A and e2m3 B fragments and adds a packed F16 accumulator.
/// Lowers to `mma.sp::ordered_metadata.sync.aligned.m16n8k64.row.col.kind::f8f6f4.f16.e5m2.e2m3.f16`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 8.7 and `sm_120a or sm_120f or sm_121a or sm_121f`.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 2-, 4-, and 4-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must be `0x4`, `0x8`, `0x9`, `0xc`, `0xd`, or `0xe`; any other value is undefined behavior.
/// `selector` must be the compile-time constant `0`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f16_e5m2_e2m3_f16(
    c: [u32; 2],
    a: [u32; 4],
    b: [u32; 4],
    metadata: u32,
    selector: u32,
) -> [u32; 2] {
    let _ = (c, a, b, metadata, selector);
    unreachable!(
        "mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f16_e5m2_e2m3_f16 called outside CUDA kernel context"
    )
}

/// Multiplies warp-distributed sparse e5m2 A and e3m2 B fragments and adds a packed F16 accumulator.
/// Lowers to `mma.sp::ordered_metadata.sync.aligned.m16n8k64.row.col.kind::f8f6f4.f16.e5m2.e3m2.f16`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 8.7 and `sm_120a or sm_120f or sm_121a or sm_121f`.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 2-, 4-, and 4-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must be `0x4`, `0x8`, `0x9`, `0xc`, `0xd`, or `0xe`; any other value is undefined behavior.
/// `selector` must be the compile-time constant `0`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f16_e5m2_e3m2_f16(
    c: [u32; 2],
    a: [u32; 4],
    b: [u32; 4],
    metadata: u32,
    selector: u32,
) -> [u32; 2] {
    let _ = (c, a, b, metadata, selector);
    unreachable!(
        "mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f16_e5m2_e3m2_f16 called outside CUDA kernel context"
    )
}

/// Multiplies warp-distributed sparse e5m2 A and e4m3 B fragments and adds a packed F16 accumulator.
/// Lowers to `mma.sp::ordered_metadata.sync.aligned.m16n8k64.row.col.kind::f8f6f4.f16.e5m2.e4m3.f16`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 8.7 and `sm_120a or sm_120f or sm_121a or sm_121f`.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 2-, 4-, and 4-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must be `0x4`, `0x8`, `0x9`, `0xc`, `0xd`, or `0xe`; any other value is undefined behavior.
/// `selector` must be the compile-time constant `0`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f16_e5m2_e4m3_f16(
    c: [u32; 2],
    a: [u32; 4],
    b: [u32; 4],
    metadata: u32,
    selector: u32,
) -> [u32; 2] {
    let _ = (c, a, b, metadata, selector);
    unreachable!(
        "mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f16_e5m2_e4m3_f16 called outside CUDA kernel context"
    )
}

/// Multiplies warp-distributed sparse e5m2 A and e5m2 B fragments and adds a packed F16 accumulator.
/// Lowers to `mma.sp::ordered_metadata.sync.aligned.m16n8k64.row.col.kind::f8f6f4.f16.e5m2.e5m2.f16`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 8.7 and `sm_120a or sm_120f or sm_121a or sm_121f`.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 2-, 4-, and 4-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must be `0x4`, `0x8`, `0x9`, `0xc`, `0xd`, or `0xe`; any other value is undefined behavior.
/// `selector` must be the compile-time constant `0`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f16_e5m2_e5m2_f16(
    c: [u32; 2],
    a: [u32; 4],
    b: [u32; 4],
    metadata: u32,
    selector: u32,
) -> [u32; 2] {
    let _ = (c, a, b, metadata, selector);
    unreachable!(
        "mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f16_e5m2_e5m2_f16 called outside CUDA kernel context"
    )
}

/// Multiplies warp-distributed sparse e2m1 A and e2m1 B fragments and adds an f32 accumulator.
/// Lowers to `mma.sp::ordered_metadata.sync.aligned.m16n8k64.row.col.kind::f8f6f4.f32.e2m1.e2m1.f32`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 8.7 and `sm_120a`.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 4-, 4-, and 4-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must be `0x4`, `0x8`, `0x9`, `0xc`, `0xd`, or `0xe`; any other value is undefined behavior.
/// `selector` must be the compile-time constant `0`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f32_e2m1_e2m1_f32(
    c: [f32; 4],
    a: [u32; 4],
    b: [u32; 4],
    metadata: u32,
    selector: u32,
) -> [f32; 4] {
    let _ = (c, a, b, metadata, selector);
    unreachable!(
        "mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f32_e2m1_e2m1_f32 called outside CUDA kernel context"
    )
}

/// Multiplies warp-distributed sparse e2m1 A and e2m3 B fragments and adds an f32 accumulator.
/// Lowers to `mma.sp::ordered_metadata.sync.aligned.m16n8k64.row.col.kind::f8f6f4.f32.e2m1.e2m3.f32`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 8.7 and `sm_120a`.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 4-, 4-, and 4-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must be `0x4`, `0x8`, `0x9`, `0xc`, `0xd`, or `0xe`; any other value is undefined behavior.
/// `selector` must be the compile-time constant `0`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f32_e2m1_e2m3_f32(
    c: [f32; 4],
    a: [u32; 4],
    b: [u32; 4],
    metadata: u32,
    selector: u32,
) -> [f32; 4] {
    let _ = (c, a, b, metadata, selector);
    unreachable!(
        "mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f32_e2m1_e2m3_f32 called outside CUDA kernel context"
    )
}

/// Multiplies warp-distributed sparse e2m1 A and e3m2 B fragments and adds an f32 accumulator.
/// Lowers to `mma.sp::ordered_metadata.sync.aligned.m16n8k64.row.col.kind::f8f6f4.f32.e2m1.e3m2.f32`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 8.7 and `sm_120a`.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 4-, 4-, and 4-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must be `0x4`, `0x8`, `0x9`, `0xc`, `0xd`, or `0xe`; any other value is undefined behavior.
/// `selector` must be the compile-time constant `0`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f32_e2m1_e3m2_f32(
    c: [f32; 4],
    a: [u32; 4],
    b: [u32; 4],
    metadata: u32,
    selector: u32,
) -> [f32; 4] {
    let _ = (c, a, b, metadata, selector);
    unreachable!(
        "mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f32_e2m1_e3m2_f32 called outside CUDA kernel context"
    )
}

/// Multiplies warp-distributed sparse e2m1 A and e4m3 B fragments and adds an f32 accumulator.
/// Lowers to `mma.sp::ordered_metadata.sync.aligned.m16n8k64.row.col.kind::f8f6f4.f32.e2m1.e4m3.f32`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 8.7 and `sm_120a`.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 4-, 4-, and 4-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must be `0x4`, `0x8`, `0x9`, `0xc`, `0xd`, or `0xe`; any other value is undefined behavior.
/// `selector` must be the compile-time constant `0`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f32_e2m1_e4m3_f32(
    c: [f32; 4],
    a: [u32; 4],
    b: [u32; 4],
    metadata: u32,
    selector: u32,
) -> [f32; 4] {
    let _ = (c, a, b, metadata, selector);
    unreachable!(
        "mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f32_e2m1_e4m3_f32 called outside CUDA kernel context"
    )
}

/// Multiplies warp-distributed sparse e2m1 A and e5m2 B fragments and adds an f32 accumulator.
/// Lowers to `mma.sp::ordered_metadata.sync.aligned.m16n8k64.row.col.kind::f8f6f4.f32.e2m1.e5m2.f32`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 8.7 and `sm_120a`.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 4-, 4-, and 4-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must be `0x4`, `0x8`, `0x9`, `0xc`, `0xd`, or `0xe`; any other value is undefined behavior.
/// `selector` must be the compile-time constant `0`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f32_e2m1_e5m2_f32(
    c: [f32; 4],
    a: [u32; 4],
    b: [u32; 4],
    metadata: u32,
    selector: u32,
) -> [f32; 4] {
    let _ = (c, a, b, metadata, selector);
    unreachable!(
        "mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f32_e2m1_e5m2_f32 called outside CUDA kernel context"
    )
}

/// Multiplies warp-distributed sparse e2m3 A and e2m1 B fragments and adds an f32 accumulator.
/// Lowers to `mma.sp::ordered_metadata.sync.aligned.m16n8k64.row.col.kind::f8f6f4.f32.e2m3.e2m1.f32`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 8.7 and `sm_120a`.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 4-, 4-, and 4-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must be `0x4`, `0x8`, `0x9`, `0xc`, `0xd`, or `0xe`; any other value is undefined behavior.
/// `selector` must be the compile-time constant `0`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f32_e2m3_e2m1_f32(
    c: [f32; 4],
    a: [u32; 4],
    b: [u32; 4],
    metadata: u32,
    selector: u32,
) -> [f32; 4] {
    let _ = (c, a, b, metadata, selector);
    unreachable!(
        "mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f32_e2m3_e2m1_f32 called outside CUDA kernel context"
    )
}

/// Multiplies warp-distributed sparse e2m3 A and e2m3 B fragments and adds an f32 accumulator.
/// Lowers to `mma.sp::ordered_metadata.sync.aligned.m16n8k64.row.col.kind::f8f6f4.f32.e2m3.e2m3.f32`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 8.7 and `sm_120a`.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 4-, 4-, and 4-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must be `0x4`, `0x8`, `0x9`, `0xc`, `0xd`, or `0xe`; any other value is undefined behavior.
/// `selector` must be the compile-time constant `0`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f32_e2m3_e2m3_f32(
    c: [f32; 4],
    a: [u32; 4],
    b: [u32; 4],
    metadata: u32,
    selector: u32,
) -> [f32; 4] {
    let _ = (c, a, b, metadata, selector);
    unreachable!(
        "mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f32_e2m3_e2m3_f32 called outside CUDA kernel context"
    )
}

/// Multiplies warp-distributed sparse e2m3 A and e3m2 B fragments and adds an f32 accumulator.
/// Lowers to `mma.sp::ordered_metadata.sync.aligned.m16n8k64.row.col.kind::f8f6f4.f32.e2m3.e3m2.f32`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 8.7 and `sm_120a`.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 4-, 4-, and 4-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must be `0x4`, `0x8`, `0x9`, `0xc`, `0xd`, or `0xe`; any other value is undefined behavior.
/// `selector` must be the compile-time constant `0`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f32_e2m3_e3m2_f32(
    c: [f32; 4],
    a: [u32; 4],
    b: [u32; 4],
    metadata: u32,
    selector: u32,
) -> [f32; 4] {
    let _ = (c, a, b, metadata, selector);
    unreachable!(
        "mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f32_e2m3_e3m2_f32 called outside CUDA kernel context"
    )
}

/// Multiplies warp-distributed sparse e2m3 A and e4m3 B fragments and adds an f32 accumulator.
/// Lowers to `mma.sp::ordered_metadata.sync.aligned.m16n8k64.row.col.kind::f8f6f4.f32.e2m3.e4m3.f32`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 8.7 and `sm_120a`.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 4-, 4-, and 4-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must be `0x4`, `0x8`, `0x9`, `0xc`, `0xd`, or `0xe`; any other value is undefined behavior.
/// `selector` must be the compile-time constant `0`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f32_e2m3_e4m3_f32(
    c: [f32; 4],
    a: [u32; 4],
    b: [u32; 4],
    metadata: u32,
    selector: u32,
) -> [f32; 4] {
    let _ = (c, a, b, metadata, selector);
    unreachable!(
        "mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f32_e2m3_e4m3_f32 called outside CUDA kernel context"
    )
}

/// Multiplies warp-distributed sparse e2m3 A and e5m2 B fragments and adds an f32 accumulator.
/// Lowers to `mma.sp::ordered_metadata.sync.aligned.m16n8k64.row.col.kind::f8f6f4.f32.e2m3.e5m2.f32`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 8.7 and `sm_120a`.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 4-, 4-, and 4-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must be `0x4`, `0x8`, `0x9`, `0xc`, `0xd`, or `0xe`; any other value is undefined behavior.
/// `selector` must be the compile-time constant `0`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f32_e2m3_e5m2_f32(
    c: [f32; 4],
    a: [u32; 4],
    b: [u32; 4],
    metadata: u32,
    selector: u32,
) -> [f32; 4] {
    let _ = (c, a, b, metadata, selector);
    unreachable!(
        "mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f32_e2m3_e5m2_f32 called outside CUDA kernel context"
    )
}

/// Multiplies warp-distributed sparse e3m2 A and e2m1 B fragments and adds an f32 accumulator.
/// Lowers to `mma.sp::ordered_metadata.sync.aligned.m16n8k64.row.col.kind::f8f6f4.f32.e3m2.e2m1.f32`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 8.7 and `sm_120a`.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 4-, 4-, and 4-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must be `0x4`, `0x8`, `0x9`, `0xc`, `0xd`, or `0xe`; any other value is undefined behavior.
/// `selector` must be the compile-time constant `0`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f32_e3m2_e2m1_f32(
    c: [f32; 4],
    a: [u32; 4],
    b: [u32; 4],
    metadata: u32,
    selector: u32,
) -> [f32; 4] {
    let _ = (c, a, b, metadata, selector);
    unreachable!(
        "mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f32_e3m2_e2m1_f32 called outside CUDA kernel context"
    )
}

/// Multiplies warp-distributed sparse e3m2 A and e2m3 B fragments and adds an f32 accumulator.
/// Lowers to `mma.sp::ordered_metadata.sync.aligned.m16n8k64.row.col.kind::f8f6f4.f32.e3m2.e2m3.f32`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 8.7 and `sm_120a`.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 4-, 4-, and 4-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must be `0x4`, `0x8`, `0x9`, `0xc`, `0xd`, or `0xe`; any other value is undefined behavior.
/// `selector` must be the compile-time constant `0`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f32_e3m2_e2m3_f32(
    c: [f32; 4],
    a: [u32; 4],
    b: [u32; 4],
    metadata: u32,
    selector: u32,
) -> [f32; 4] {
    let _ = (c, a, b, metadata, selector);
    unreachable!(
        "mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f32_e3m2_e2m3_f32 called outside CUDA kernel context"
    )
}

/// Multiplies warp-distributed sparse e3m2 A and e3m2 B fragments and adds an f32 accumulator.
/// Lowers to `mma.sp::ordered_metadata.sync.aligned.m16n8k64.row.col.kind::f8f6f4.f32.e3m2.e3m2.f32`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 8.7 and `sm_120a`.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 4-, 4-, and 4-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must be `0x4`, `0x8`, `0x9`, `0xc`, `0xd`, or `0xe`; any other value is undefined behavior.
/// `selector` must be the compile-time constant `0`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f32_e3m2_e3m2_f32(
    c: [f32; 4],
    a: [u32; 4],
    b: [u32; 4],
    metadata: u32,
    selector: u32,
) -> [f32; 4] {
    let _ = (c, a, b, metadata, selector);
    unreachable!(
        "mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f32_e3m2_e3m2_f32 called outside CUDA kernel context"
    )
}

/// Multiplies warp-distributed sparse e3m2 A and e4m3 B fragments and adds an f32 accumulator.
/// Lowers to `mma.sp::ordered_metadata.sync.aligned.m16n8k64.row.col.kind::f8f6f4.f32.e3m2.e4m3.f32`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 8.7 and `sm_120a`.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 4-, 4-, and 4-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must be `0x4`, `0x8`, `0x9`, `0xc`, `0xd`, or `0xe`; any other value is undefined behavior.
/// `selector` must be the compile-time constant `0`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f32_e3m2_e4m3_f32(
    c: [f32; 4],
    a: [u32; 4],
    b: [u32; 4],
    metadata: u32,
    selector: u32,
) -> [f32; 4] {
    let _ = (c, a, b, metadata, selector);
    unreachable!(
        "mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f32_e3m2_e4m3_f32 called outside CUDA kernel context"
    )
}

/// Multiplies warp-distributed sparse e3m2 A and e5m2 B fragments and adds an f32 accumulator.
/// Lowers to `mma.sp::ordered_metadata.sync.aligned.m16n8k64.row.col.kind::f8f6f4.f32.e3m2.e5m2.f32`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 8.7 and `sm_120a`.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 4-, 4-, and 4-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must be `0x4`, `0x8`, `0x9`, `0xc`, `0xd`, or `0xe`; any other value is undefined behavior.
/// `selector` must be the compile-time constant `0`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f32_e3m2_e5m2_f32(
    c: [f32; 4],
    a: [u32; 4],
    b: [u32; 4],
    metadata: u32,
    selector: u32,
) -> [f32; 4] {
    let _ = (c, a, b, metadata, selector);
    unreachable!(
        "mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f32_e3m2_e5m2_f32 called outside CUDA kernel context"
    )
}

/// Multiplies warp-distributed sparse e4m3 A and e2m1 B fragments and adds an f32 accumulator.
/// Lowers to `mma.sp::ordered_metadata.sync.aligned.m16n8k64.row.col.kind::f8f6f4.f32.e4m3.e2m1.f32`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 8.7 and `sm_120a`.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 4-, 4-, and 4-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must be `0x4`, `0x8`, `0x9`, `0xc`, `0xd`, or `0xe`; any other value is undefined behavior.
/// `selector` must be the compile-time constant `0`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f32_e4m3_e2m1_f32(
    c: [f32; 4],
    a: [u32; 4],
    b: [u32; 4],
    metadata: u32,
    selector: u32,
) -> [f32; 4] {
    let _ = (c, a, b, metadata, selector);
    unreachable!(
        "mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f32_e4m3_e2m1_f32 called outside CUDA kernel context"
    )
}

/// Multiplies warp-distributed sparse e4m3 A and e2m3 B fragments and adds an f32 accumulator.
/// Lowers to `mma.sp::ordered_metadata.sync.aligned.m16n8k64.row.col.kind::f8f6f4.f32.e4m3.e2m3.f32`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 8.7 and `sm_120a`.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 4-, 4-, and 4-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must be `0x4`, `0x8`, `0x9`, `0xc`, `0xd`, or `0xe`; any other value is undefined behavior.
/// `selector` must be the compile-time constant `0`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f32_e4m3_e2m3_f32(
    c: [f32; 4],
    a: [u32; 4],
    b: [u32; 4],
    metadata: u32,
    selector: u32,
) -> [f32; 4] {
    let _ = (c, a, b, metadata, selector);
    unreachable!(
        "mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f32_e4m3_e2m3_f32 called outside CUDA kernel context"
    )
}

/// Multiplies warp-distributed sparse e4m3 A and e3m2 B fragments and adds an f32 accumulator.
/// Lowers to `mma.sp::ordered_metadata.sync.aligned.m16n8k64.row.col.kind::f8f6f4.f32.e4m3.e3m2.f32`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 8.7 and `sm_120a`.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 4-, 4-, and 4-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must be `0x4`, `0x8`, `0x9`, `0xc`, `0xd`, or `0xe`; any other value is undefined behavior.
/// `selector` must be the compile-time constant `0`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f32_e4m3_e3m2_f32(
    c: [f32; 4],
    a: [u32; 4],
    b: [u32; 4],
    metadata: u32,
    selector: u32,
) -> [f32; 4] {
    let _ = (c, a, b, metadata, selector);
    unreachable!(
        "mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f32_e4m3_e3m2_f32 called outside CUDA kernel context"
    )
}

/// Multiplies warp-distributed sparse e4m3 A and e4m3 B fragments and adds an f32 accumulator.
/// Lowers to `mma.sp::ordered_metadata.sync.aligned.m16n8k64.row.col.kind::f8f6f4.f32.e4m3.e4m3.f32`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 8.7 and `sm_120a`.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 4-, 4-, and 4-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must be `0x4`, `0x8`, `0x9`, `0xc`, `0xd`, or `0xe`; any other value is undefined behavior.
/// `selector` must be the compile-time constant `0`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f32_e4m3_e4m3_f32(
    c: [f32; 4],
    a: [u32; 4],
    b: [u32; 4],
    metadata: u32,
    selector: u32,
) -> [f32; 4] {
    let _ = (c, a, b, metadata, selector);
    unreachable!(
        "mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f32_e4m3_e4m3_f32 called outside CUDA kernel context"
    )
}

/// Multiplies warp-distributed sparse e4m3 A and e5m2 B fragments and adds an f32 accumulator.
/// Lowers to `mma.sp::ordered_metadata.sync.aligned.m16n8k64.row.col.kind::f8f6f4.f32.e4m3.e5m2.f32`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 8.7 and `sm_120a`.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 4-, 4-, and 4-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must be `0x4`, `0x8`, `0x9`, `0xc`, `0xd`, or `0xe`; any other value is undefined behavior.
/// `selector` must be the compile-time constant `0`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f32_e4m3_e5m2_f32(
    c: [f32; 4],
    a: [u32; 4],
    b: [u32; 4],
    metadata: u32,
    selector: u32,
) -> [f32; 4] {
    let _ = (c, a, b, metadata, selector);
    unreachable!(
        "mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f32_e4m3_e5m2_f32 called outside CUDA kernel context"
    )
}

/// Multiplies warp-distributed sparse e5m2 A and e2m1 B fragments and adds an f32 accumulator.
/// Lowers to `mma.sp::ordered_metadata.sync.aligned.m16n8k64.row.col.kind::f8f6f4.f32.e5m2.e2m1.f32`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 8.7 and `sm_120a`.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 4-, 4-, and 4-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must be `0x4`, `0x8`, `0x9`, `0xc`, `0xd`, or `0xe`; any other value is undefined behavior.
/// `selector` must be the compile-time constant `0`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f32_e5m2_e2m1_f32(
    c: [f32; 4],
    a: [u32; 4],
    b: [u32; 4],
    metadata: u32,
    selector: u32,
) -> [f32; 4] {
    let _ = (c, a, b, metadata, selector);
    unreachable!(
        "mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f32_e5m2_e2m1_f32 called outside CUDA kernel context"
    )
}

/// Multiplies warp-distributed sparse e5m2 A and e2m3 B fragments and adds an f32 accumulator.
/// Lowers to `mma.sp::ordered_metadata.sync.aligned.m16n8k64.row.col.kind::f8f6f4.f32.e5m2.e2m3.f32`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 8.7 and `sm_120a`.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 4-, 4-, and 4-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must be `0x4`, `0x8`, `0x9`, `0xc`, `0xd`, or `0xe`; any other value is undefined behavior.
/// `selector` must be the compile-time constant `0`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f32_e5m2_e2m3_f32(
    c: [f32; 4],
    a: [u32; 4],
    b: [u32; 4],
    metadata: u32,
    selector: u32,
) -> [f32; 4] {
    let _ = (c, a, b, metadata, selector);
    unreachable!(
        "mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f32_e5m2_e2m3_f32 called outside CUDA kernel context"
    )
}

/// Multiplies warp-distributed sparse e5m2 A and e3m2 B fragments and adds an f32 accumulator.
/// Lowers to `mma.sp::ordered_metadata.sync.aligned.m16n8k64.row.col.kind::f8f6f4.f32.e5m2.e3m2.f32`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 8.7 and `sm_120a`.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 4-, 4-, and 4-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must be `0x4`, `0x8`, `0x9`, `0xc`, `0xd`, or `0xe`; any other value is undefined behavior.
/// `selector` must be the compile-time constant `0`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f32_e5m2_e3m2_f32(
    c: [f32; 4],
    a: [u32; 4],
    b: [u32; 4],
    metadata: u32,
    selector: u32,
) -> [f32; 4] {
    let _ = (c, a, b, metadata, selector);
    unreachable!(
        "mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f32_e5m2_e3m2_f32 called outside CUDA kernel context"
    )
}

/// Multiplies warp-distributed sparse e5m2 A and e4m3 B fragments and adds an f32 accumulator.
/// Lowers to `mma.sp::ordered_metadata.sync.aligned.m16n8k64.row.col.kind::f8f6f4.f32.e5m2.e4m3.f32`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 8.7 and `sm_120a`.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 4-, 4-, and 4-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must be `0x4`, `0x8`, `0x9`, `0xc`, `0xd`, or `0xe`; any other value is undefined behavior.
/// `selector` must be the compile-time constant `0`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f32_e5m2_e4m3_f32(
    c: [f32; 4],
    a: [u32; 4],
    b: [u32; 4],
    metadata: u32,
    selector: u32,
) -> [f32; 4] {
    let _ = (c, a, b, metadata, selector);
    unreachable!(
        "mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f32_e5m2_e4m3_f32 called outside CUDA kernel context"
    )
}

/// Multiplies warp-distributed sparse e5m2 A and e5m2 B fragments and adds an f32 accumulator.
/// Lowers to `mma.sp::ordered_metadata.sync.aligned.m16n8k64.row.col.kind::f8f6f4.f32.e5m2.e5m2.f32`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 8.7 and `sm_120a`.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 4-, 4-, and 4-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must be `0x4`, `0x8`, `0x9`, `0xc`, `0xd`, or `0xe`; any other value is undefined behavior.
/// `selector` must be the compile-time constant `0`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f32_e5m2_e5m2_f32(
    c: [f32; 4],
    a: [u32; 4],
    b: [u32; 4],
    metadata: u32,
    selector: u32,
) -> [f32; 4] {
    let _ = (c, a, b, metadata, selector);
    unreachable!(
        "mma_sp_ordered_metadata_m16n8k64_kind_f8f6f4_f32_e5m2_e5m2_f32 called outside CUDA kernel context"
    )
}

/// Multiplies warp-distributed sparse signed A and signed B INT4 fragments with ordered sparsity metadata and adds a wrapping s32 accumulator.
/// Lowers to `mma.sp::ordered_metadata.sync.aligned.m16n8k64.row.col.s32.s4.s4.s32`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 8.5 and `sm_80+`.
/// Signed accumulator overflow wraps.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 4-, 2-, and 2-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must be `0x4`, `0x8`, `0x9`, `0xc`, `0xd`, or `0xe`; any other value is undefined behavior.
/// `selector` must be the compile-time constant `0` or `1`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_ordered_metadata_m16n8k64_s32_s4(
    c: [i32; 4],
    a: [u32; 2],
    b: [u32; 2],
    metadata: u32,
    selector: u32,
) -> [i32; 4] {
    let _ = (c, a, b, metadata, selector);
    unreachable!("mma_sp_ordered_metadata_m16n8k64_s32_s4 called outside CUDA kernel context")
}

/// Multiplies warp-distributed sparse signed A and signed B INT4 fragments with ordered sparsity metadata and adds a saturating s32 accumulator.
/// Lowers to `mma.sp::ordered_metadata.sync.aligned.m16n8k64.row.col.satfinite.s32.s4.s4.s32`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 8.5 and `sm_80+`.
/// Signed accumulator overflow clamps to the finite `i32` range.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 4-, 2-, and 2-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must be `0x4`, `0x8`, `0x9`, `0xc`, `0xd`, or `0xe`; any other value is undefined behavior.
/// `selector` must be the compile-time constant `0` or `1`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_ordered_metadata_m16n8k64_s32_s4_satfinite(
    c: [i32; 4],
    a: [u32; 2],
    b: [u32; 2],
    metadata: u32,
    selector: u32,
) -> [i32; 4] {
    let _ = (c, a, b, metadata, selector);
    unreachable!(
        "mma_sp_ordered_metadata_m16n8k64_s32_s4_satfinite called outside CUDA kernel context"
    )
}

/// Multiplies warp-distributed sparse signed A and unsigned B INT4 fragments with ordered sparsity metadata and adds a wrapping s32 accumulator.
/// Lowers to `mma.sp::ordered_metadata.sync.aligned.m16n8k64.row.col.s32.s4.u4.s32`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 8.5 and `sm_80+`.
/// Signed accumulator overflow wraps.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 4-, 2-, and 2-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must be `0x4`, `0x8`, `0x9`, `0xc`, `0xd`, or `0xe`; any other value is undefined behavior.
/// `selector` must be the compile-time constant `0` or `1`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_ordered_metadata_m16n8k64_s32_s4_u4(
    c: [i32; 4],
    a: [u32; 2],
    b: [u32; 2],
    metadata: u32,
    selector: u32,
) -> [i32; 4] {
    let _ = (c, a, b, metadata, selector);
    unreachable!("mma_sp_ordered_metadata_m16n8k64_s32_s4_u4 called outside CUDA kernel context")
}

/// Multiplies warp-distributed sparse signed A and unsigned B INT4 fragments with ordered sparsity metadata and adds a saturating s32 accumulator.
/// Lowers to `mma.sp::ordered_metadata.sync.aligned.m16n8k64.row.col.satfinite.s32.s4.u4.s32`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 8.5 and `sm_80+`.
/// Signed accumulator overflow clamps to the finite `i32` range.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 4-, 2-, and 2-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must be `0x4`, `0x8`, `0x9`, `0xc`, `0xd`, or `0xe`; any other value is undefined behavior.
/// `selector` must be the compile-time constant `0` or `1`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_ordered_metadata_m16n8k64_s32_s4_u4_satfinite(
    c: [i32; 4],
    a: [u32; 2],
    b: [u32; 2],
    metadata: u32,
    selector: u32,
) -> [i32; 4] {
    let _ = (c, a, b, metadata, selector);
    unreachable!(
        "mma_sp_ordered_metadata_m16n8k64_s32_s4_u4_satfinite called outside CUDA kernel context"
    )
}

/// Multiplies warp-distributed sparse signed A and signed B INT8 fragments with ordered sparsity metadata and adds a wrapping s32 accumulator.
/// Lowers to `mma.sp::ordered_metadata.sync.aligned.m16n8k64.row.col.s32.s8.s8.s32`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 8.5 and `sm_80+`.
/// Signed accumulator overflow wraps.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 4-, 4-, and 4-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must be `0x4`, `0x8`, `0x9`, `0xc`, `0xd`, or `0xe`; any other value is undefined behavior.
/// `selector` must be the compile-time constant `0`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_ordered_metadata_m16n8k64_s32_s8(
    c: [i32; 4],
    a: [u32; 4],
    b: [u32; 4],
    metadata: u32,
    selector: u32,
) -> [i32; 4] {
    let _ = (c, a, b, metadata, selector);
    unreachable!("mma_sp_ordered_metadata_m16n8k64_s32_s8 called outside CUDA kernel context")
}

/// Multiplies warp-distributed sparse signed A and signed B INT8 fragments with ordered sparsity metadata and adds a saturating s32 accumulator.
/// Lowers to `mma.sp::ordered_metadata.sync.aligned.m16n8k64.row.col.satfinite.s32.s8.s8.s32`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 8.5 and `sm_80+`.
/// Signed accumulator overflow clamps to the finite `i32` range.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 4-, 4-, and 4-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must be `0x4`, `0x8`, `0x9`, `0xc`, `0xd`, or `0xe`; any other value is undefined behavior.
/// `selector` must be the compile-time constant `0`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_ordered_metadata_m16n8k64_s32_s8_satfinite(
    c: [i32; 4],
    a: [u32; 4],
    b: [u32; 4],
    metadata: u32,
    selector: u32,
) -> [i32; 4] {
    let _ = (c, a, b, metadata, selector);
    unreachable!(
        "mma_sp_ordered_metadata_m16n8k64_s32_s8_satfinite called outside CUDA kernel context"
    )
}

/// Multiplies warp-distributed sparse signed A and unsigned B INT8 fragments with ordered sparsity metadata and adds a wrapping s32 accumulator.
/// Lowers to `mma.sp::ordered_metadata.sync.aligned.m16n8k64.row.col.s32.s8.u8.s32`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 8.5 and `sm_80+`.
/// Signed accumulator overflow wraps.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 4-, 4-, and 4-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must be `0x4`, `0x8`, `0x9`, `0xc`, `0xd`, or `0xe`; any other value is undefined behavior.
/// `selector` must be the compile-time constant `0`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_ordered_metadata_m16n8k64_s32_s8_u8(
    c: [i32; 4],
    a: [u32; 4],
    b: [u32; 4],
    metadata: u32,
    selector: u32,
) -> [i32; 4] {
    let _ = (c, a, b, metadata, selector);
    unreachable!("mma_sp_ordered_metadata_m16n8k64_s32_s8_u8 called outside CUDA kernel context")
}

/// Multiplies warp-distributed sparse signed A and unsigned B INT8 fragments with ordered sparsity metadata and adds a saturating s32 accumulator.
/// Lowers to `mma.sp::ordered_metadata.sync.aligned.m16n8k64.row.col.satfinite.s32.s8.u8.s32`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 8.5 and `sm_80+`.
/// Signed accumulator overflow clamps to the finite `i32` range.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 4-, 4-, and 4-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must be `0x4`, `0x8`, `0x9`, `0xc`, `0xd`, or `0xe`; any other value is undefined behavior.
/// `selector` must be the compile-time constant `0`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_ordered_metadata_m16n8k64_s32_s8_u8_satfinite(
    c: [i32; 4],
    a: [u32; 4],
    b: [u32; 4],
    metadata: u32,
    selector: u32,
) -> [i32; 4] {
    let _ = (c, a, b, metadata, selector);
    unreachable!(
        "mma_sp_ordered_metadata_m16n8k64_s32_s8_u8_satfinite called outside CUDA kernel context"
    )
}

/// Multiplies warp-distributed sparse unsigned A and unsigned B INT4 fragments with ordered sparsity metadata and adds a wrapping s32 accumulator.
/// Lowers to `mma.sp::ordered_metadata.sync.aligned.m16n8k64.row.col.s32.u4.u4.s32`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 8.5 and `sm_80+`.
/// Signed accumulator overflow wraps.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 4-, 2-, and 2-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must be `0x4`, `0x8`, `0x9`, `0xc`, `0xd`, or `0xe`; any other value is undefined behavior.
/// `selector` must be the compile-time constant `0` or `1`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_ordered_metadata_m16n8k64_s32_u4(
    c: [i32; 4],
    a: [u32; 2],
    b: [u32; 2],
    metadata: u32,
    selector: u32,
) -> [i32; 4] {
    let _ = (c, a, b, metadata, selector);
    unreachable!("mma_sp_ordered_metadata_m16n8k64_s32_u4 called outside CUDA kernel context")
}

/// Multiplies warp-distributed sparse unsigned A and signed B INT4 fragments with ordered sparsity metadata and adds a wrapping s32 accumulator.
/// Lowers to `mma.sp::ordered_metadata.sync.aligned.m16n8k64.row.col.s32.u4.s4.s32`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 8.5 and `sm_80+`.
/// Signed accumulator overflow wraps.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 4-, 2-, and 2-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must be `0x4`, `0x8`, `0x9`, `0xc`, `0xd`, or `0xe`; any other value is undefined behavior.
/// `selector` must be the compile-time constant `0` or `1`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_ordered_metadata_m16n8k64_s32_u4_s4(
    c: [i32; 4],
    a: [u32; 2],
    b: [u32; 2],
    metadata: u32,
    selector: u32,
) -> [i32; 4] {
    let _ = (c, a, b, metadata, selector);
    unreachable!("mma_sp_ordered_metadata_m16n8k64_s32_u4_s4 called outside CUDA kernel context")
}

/// Multiplies warp-distributed sparse unsigned A and signed B INT4 fragments with ordered sparsity metadata and adds a saturating s32 accumulator.
/// Lowers to `mma.sp::ordered_metadata.sync.aligned.m16n8k64.row.col.satfinite.s32.u4.s4.s32`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 8.5 and `sm_80+`.
/// Signed accumulator overflow clamps to the finite `i32` range.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 4-, 2-, and 2-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must be `0x4`, `0x8`, `0x9`, `0xc`, `0xd`, or `0xe`; any other value is undefined behavior.
/// `selector` must be the compile-time constant `0` or `1`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_ordered_metadata_m16n8k64_s32_u4_s4_satfinite(
    c: [i32; 4],
    a: [u32; 2],
    b: [u32; 2],
    metadata: u32,
    selector: u32,
) -> [i32; 4] {
    let _ = (c, a, b, metadata, selector);
    unreachable!(
        "mma_sp_ordered_metadata_m16n8k64_s32_u4_s4_satfinite called outside CUDA kernel context"
    )
}

/// Multiplies warp-distributed sparse unsigned A and unsigned B INT4 fragments with ordered sparsity metadata and adds a saturating s32 accumulator.
/// Lowers to `mma.sp::ordered_metadata.sync.aligned.m16n8k64.row.col.satfinite.s32.u4.u4.s32`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 8.5 and `sm_80+`.
/// Signed accumulator overflow clamps to the finite `i32` range.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 4-, 2-, and 2-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must be `0x4`, `0x8`, `0x9`, `0xc`, `0xd`, or `0xe`; any other value is undefined behavior.
/// `selector` must be the compile-time constant `0` or `1`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_ordered_metadata_m16n8k64_s32_u4_satfinite(
    c: [i32; 4],
    a: [u32; 2],
    b: [u32; 2],
    metadata: u32,
    selector: u32,
) -> [i32; 4] {
    let _ = (c, a, b, metadata, selector);
    unreachable!(
        "mma_sp_ordered_metadata_m16n8k64_s32_u4_satfinite called outside CUDA kernel context"
    )
}

/// Multiplies warp-distributed sparse unsigned A and unsigned B INT8 fragments with ordered sparsity metadata and adds a wrapping s32 accumulator.
/// Lowers to `mma.sp::ordered_metadata.sync.aligned.m16n8k64.row.col.s32.u8.u8.s32`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 8.5 and `sm_80+`.
/// Signed accumulator overflow wraps.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 4-, 4-, and 4-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must be `0x4`, `0x8`, `0x9`, `0xc`, `0xd`, or `0xe`; any other value is undefined behavior.
/// `selector` must be the compile-time constant `0`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_ordered_metadata_m16n8k64_s32_u8(
    c: [i32; 4],
    a: [u32; 4],
    b: [u32; 4],
    metadata: u32,
    selector: u32,
) -> [i32; 4] {
    let _ = (c, a, b, metadata, selector);
    unreachable!("mma_sp_ordered_metadata_m16n8k64_s32_u8 called outside CUDA kernel context")
}

/// Multiplies warp-distributed sparse unsigned A and signed B INT8 fragments with ordered sparsity metadata and adds a wrapping s32 accumulator.
/// Lowers to `mma.sp::ordered_metadata.sync.aligned.m16n8k64.row.col.s32.u8.s8.s32`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 8.5 and `sm_80+`.
/// Signed accumulator overflow wraps.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 4-, 4-, and 4-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must be `0x4`, `0x8`, `0x9`, `0xc`, `0xd`, or `0xe`; any other value is undefined behavior.
/// `selector` must be the compile-time constant `0`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_ordered_metadata_m16n8k64_s32_u8_s8(
    c: [i32; 4],
    a: [u32; 4],
    b: [u32; 4],
    metadata: u32,
    selector: u32,
) -> [i32; 4] {
    let _ = (c, a, b, metadata, selector);
    unreachable!("mma_sp_ordered_metadata_m16n8k64_s32_u8_s8 called outside CUDA kernel context")
}

/// Multiplies warp-distributed sparse unsigned A and signed B INT8 fragments with ordered sparsity metadata and adds a saturating s32 accumulator.
/// Lowers to `mma.sp::ordered_metadata.sync.aligned.m16n8k64.row.col.satfinite.s32.u8.s8.s32`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 8.5 and `sm_80+`.
/// Signed accumulator overflow clamps to the finite `i32` range.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 4-, 4-, and 4-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must be `0x4`, `0x8`, `0x9`, `0xc`, `0xd`, or `0xe`; any other value is undefined behavior.
/// `selector` must be the compile-time constant `0`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_ordered_metadata_m16n8k64_s32_u8_s8_satfinite(
    c: [i32; 4],
    a: [u32; 4],
    b: [u32; 4],
    metadata: u32,
    selector: u32,
) -> [i32; 4] {
    let _ = (c, a, b, metadata, selector);
    unreachable!(
        "mma_sp_ordered_metadata_m16n8k64_s32_u8_s8_satfinite called outside CUDA kernel context"
    )
}

/// Multiplies warp-distributed sparse unsigned A and unsigned B INT8 fragments with ordered sparsity metadata and adds a saturating s32 accumulator.
/// Lowers to `mma.sp::ordered_metadata.sync.aligned.m16n8k64.row.col.satfinite.s32.u8.u8.s32`. C, A, B, and metadata are this lane's PTX fragments.
/// Requires PTX 8.5 and `sm_80+`.
/// Signed accumulator overflow clamps to the finite `i32` range.
///
/// # Safety
/// All 32 lanes must execute the same instruction with the same qualifiers, and no lane may have exited.
/// `c`, `a`, and `b` must contain this lane's 4-, 4-, and 4-register fragments; `metadata` must contain its sparse metadata.
/// Every 4-bit metadata group must be `0x4`, `0x8`, `0x9`, `0xc`, `0xd`, or `0xe`; any other value is undefined behavior.
/// `selector` must be the compile-time constant `0`.
/// See the [PTX sparse MMA fragment layouts](https://docs.nvidia.com/cuda/parallel-thread-execution/#warp-level-matrix-instructions-mma-sp).
/// This register-only operation is not a memory fence.
#[must_use]
#[inline(never)]
pub unsafe fn mma_sp_ordered_metadata_m16n8k64_s32_u8_satfinite(
    c: [i32; 4],
    a: [u32; 4],
    b: [u32; 4],
    metadata: u32,
    selector: u32,
) -> [i32; 4] {
    let _ = (c, a, b, metadata, selector);
    unreachable!(
        "mma_sp_ordered_metadata_m16n8k64_s32_u8_satfinite called outside CUDA kernel context"
    )
}
