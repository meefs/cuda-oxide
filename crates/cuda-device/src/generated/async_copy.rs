/*
 * SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
 * SPDX-License-Identifier: Apache-2.0
 */

// @generated by cuda-intrinsics-gen 0.1.0. DO NOT EDIT.
// Catalog schema/version: 44/0.1.0; intrinsic ABI: v1; catalog SHA-256: 1f72d053300ad70672b9cf851743dd05e0904f7222828eecd53d5ca88a4a0088.
// LLVM source: 1cb4e3833c1919c2e6fb579a23ac0e2b22587b7e.

// Included inside `cuda_device::async_copy` to keep existing paths stable.

/// Starts a sixteen-byte cache-all asynchronous copy from global to shared memory.
/// Uses the cache-all policy and copies 16 bytes.
///
/// # Safety
/// `shared_dst` must point to 16 writable bytes in shared memory and be aligned to 16 bytes.
/// `global_src` must point to 16 readable bytes in global memory and be aligned to 16 bytes.
/// Both ranges must remain valid, the source must remain unchanged, and the destination must not be accessed until this copy completes.
/// The issuing thread must complete the copy. Synchronize threads afterward before another thread accesses the destination.
/// User-authored completion assembly must include a compiler memory clobber.
#[inline(never)]
pub unsafe fn cp_async_ca_16(_shared_dst: *mut u32, _global_src: *const u32) {
    unreachable!("cp_async_ca_16 called outside CUDA kernel context")
}

/// Starts a four-byte cache-all asynchronous copy from global to shared memory.
/// Uses the cache-all policy and copies 4 bytes.
///
/// # Safety
/// `shared_dst` must point to 4 writable bytes in shared memory and be aligned to 4 bytes.
/// `global_src` must point to 4 readable bytes in global memory and be aligned to 4 bytes.
/// Both ranges must remain valid, the source must remain unchanged, and the destination must not be accessed until this copy completes.
/// The issuing thread must complete the copy. Synchronize threads afterward before another thread accesses the destination.
/// User-authored completion assembly must include a compiler memory clobber.
#[inline(never)]
pub unsafe fn cp_async_ca_4(_shared_dst: *mut u32, _global_src: *const u32) {
    unreachable!("cp_async_ca_4 called outside CUDA kernel context")
}

/// Starts an eight-byte cache-all asynchronous copy from global to shared memory.
/// Uses the cache-all policy and copies 8 bytes.
///
/// # Safety
/// `shared_dst` must point to 8 writable bytes in shared memory and be aligned to 8 bytes.
/// `global_src` must point to 8 readable bytes in global memory and be aligned to 8 bytes.
/// Both ranges must remain valid, the source must remain unchanged, and the destination must not be accessed until this copy completes.
/// The issuing thread must complete the copy. Synchronize threads afterward before another thread accesses the destination.
/// User-authored completion assembly must include a compiler memory clobber.
#[inline(never)]
pub unsafe fn cp_async_ca_8(_shared_dst: *mut u32, _global_src: *const u32) {
    unreachable!("cp_async_ca_8 called outside CUDA kernel context")
}

/// Starts a sixteen-byte cache-all asynchronous copy and zero-fills bytes beyond the runtime source size.
/// Uses the cache-all policy and copies 16 bytes.
/// Bytes after `src_size` are filled with zero; `src_size` must be at most 16.
///
/// # Safety
/// `shared_dst` must point to 16 writable bytes in shared memory and be aligned to 16 bytes.
/// `global_src` must point to `src_size` readable bytes in global memory and be aligned to 16 bytes.
/// Both ranges must remain valid, the source must remain unchanged, and the destination must not be accessed until this copy completes.
/// The issuing thread must complete the copy. Synchronize threads afterward before another thread accesses the destination.
/// User-authored completion assembly must include a compiler memory clobber.
#[inline(never)]
pub unsafe fn cp_async_ca_zfill_16(_shared_dst: *mut u32, _global_src: *const u8, _src_size: u32) {
    unreachable!("cp_async_ca_zfill_16 called outside CUDA kernel context")
}

/// Starts a four-byte cache-all asynchronous copy and zero-fills bytes beyond the runtime source size.
/// Uses the cache-all policy and copies 4 bytes.
/// Bytes after `src_size` are filled with zero; `src_size` must be at most 4.
///
/// # Safety
/// `shared_dst` must point to 4 writable bytes in shared memory and be aligned to 4 bytes.
/// `global_src` must point to `src_size` readable bytes in global memory and be aligned to 4 bytes.
/// Both ranges must remain valid, the source must remain unchanged, and the destination must not be accessed until this copy completes.
/// The issuing thread must complete the copy. Synchronize threads afterward before another thread accesses the destination.
/// User-authored completion assembly must include a compiler memory clobber.
#[inline(never)]
pub unsafe fn cp_async_ca_zfill_4(_shared_dst: *mut u32, _global_src: *const u8, _src_size: u32) {
    unreachable!("cp_async_ca_zfill_4 called outside CUDA kernel context")
}

/// Starts an eight-byte cache-all asynchronous copy and zero-fills bytes beyond the runtime source size.
/// Uses the cache-all policy and copies 8 bytes.
/// Bytes after `src_size` are filled with zero; `src_size` must be at most 8.
///
/// # Safety
/// `shared_dst` must point to 8 writable bytes in shared memory and be aligned to 8 bytes.
/// `global_src` must point to `src_size` readable bytes in global memory and be aligned to 8 bytes.
/// Both ranges must remain valid, the source must remain unchanged, and the destination must not be accessed until this copy completes.
/// The issuing thread must complete the copy. Synchronize threads afterward before another thread accesses the destination.
/// User-authored completion assembly must include a compiler memory clobber.
#[inline(never)]
pub unsafe fn cp_async_ca_zfill_8(_shared_dst: *mut u32, _global_src: *const u8, _src_size: u32) {
    unreachable!("cp_async_ca_zfill_8 called outside CUDA kernel context")
}

/// Starts a sixteen-byte cache-global asynchronous copy from global to shared memory.
/// Uses the cache-global policy and copies 16 bytes.
///
/// # Safety
/// `shared_dst` must point to 16 writable bytes in shared memory and be aligned to 16 bytes.
/// `global_src` must point to 16 readable bytes in global memory and be aligned to 16 bytes.
/// Both ranges must remain valid, the source must remain unchanged, and the destination must not be accessed until this copy completes.
/// The issuing thread must complete the copy. Synchronize threads afterward before another thread accesses the destination.
/// User-authored completion assembly must include a compiler memory clobber.
#[inline(never)]
pub unsafe fn cp_async_cg_16(_shared_dst: *mut u32, _global_src: *const u32) {
    unreachable!("cp_async_cg_16 called outside CUDA kernel context")
}

/// Starts a sixteen-byte cache-global asynchronous copy and zero-fills bytes beyond the runtime source size.
/// Uses the cache-global policy and copies 16 bytes.
/// Bytes after `src_size` are filled with zero; `src_size` must be at most 16.
///
/// # Safety
/// `shared_dst` must point to 16 writable bytes in shared memory and be aligned to 16 bytes.
/// `global_src` must point to `src_size` readable bytes in global memory and be aligned to 16 bytes.
/// Both ranges must remain valid, the source must remain unchanged, and the destination must not be accessed until this copy completes.
/// The issuing thread must complete the copy. Synchronize threads afterward before another thread accesses the destination.
/// User-authored completion assembly must include a compiler memory clobber.
#[inline(never)]
pub unsafe fn cp_async_cg_zfill_16(_shared_dst: *mut u32, _global_src: *const u8, _src_size: u32) {
    unreachable!("cp_async_cg_zfill_16 called outside CUDA kernel context")
}

/// Commits this thread's uncommitted asynchronous copies as one group.
///
/// # Safety
/// This commits only copies issued by the executing thread and does not wait for completion.
#[inline(never)]
pub unsafe fn cp_async_commit_group() {
    unreachable!("cp_async_commit_group called outside CUDA kernel context")
}

/// Waits for all asynchronous copy groups issued by this thread.
///
/// # Safety
/// This waits only for copies issued by this thread. Synchronize threads before another thread accesses a completed destination.
#[inline(never)]
pub unsafe fn cp_async_wait_all() {
    unreachable!("cp_async_wait_all called outside CUDA kernel context")
}

/// Waits until at most the compile-time constant number of recent asynchronous copy groups remain pending.
///
/// # Safety
/// `max_pending` must be a compile-time constant. Access only destinations whose copy groups this wait completes.
#[inline(never)]
pub unsafe fn cp_async_wait_group(_max_pending: u32) {
    unreachable!("cp_async_wait_group called outside CUDA kernel context")
}

/// Associates this thread's prior asynchronous copies with a shared-memory barrier using balanced pending-count accounting.
/// Lowers to `cp.async.mbarrier.arrive.b64 [<address>];`.
///
/// # Safety
/// `barrier` must point to a live, initialized, eight-byte-aligned mbarrier object in shared memory.
/// This thread must have issued the `cp.async` operations being associated with it.
/// The object must remain valid until those operations complete.
/// This form increments the pending count before scheduling the asynchronous arrival.
/// That increment must not exceed the barrier's pending-count limit.
#[inline(never)]
pub unsafe fn cp_async_mbarrier_arrive(_barrier: *mut crate::barrier::Barrier) {
    unreachable!("cp_async_mbarrier_arrive called outside CUDA kernel context")
}

/// Associates this thread's prior asynchronous copies with a shared-memory barrier without incrementing its pending count.
/// Lowers to `cp.async.mbarrier.arrive.noinc.b64 [<address>];`.
///
/// # Safety
/// `barrier` must point to a live, initialized, eight-byte-aligned mbarrier object in shared memory.
/// This thread must have issued the `cp.async` operations being associated with it.
/// The object must remain valid until those operations complete.
/// The initial pending count must already include the asynchronous arrival.
#[inline(never)]
pub unsafe fn cp_async_mbarrier_arrive_noinc(_barrier: *mut crate::barrier::Barrier) {
    unreachable!("cp_async_mbarrier_arrive_noinc called outside CUDA kernel context")
}

/// Associates this thread's prior asynchronous copies with a shared-address barrier without incrementing its pending count.
/// Lowers to `cp.async.mbarrier.arrive.noinc.shared.b64 [<address>];`.
///
/// # Safety
/// `barrier` must point to a live, initialized, eight-byte-aligned mbarrier object in shared memory.
/// This thread must have issued the `cp.async` operations being associated with it.
/// The object must remain valid until those operations complete.
/// The initial pending count must already include the asynchronous arrival.
#[inline(never)]
pub unsafe fn cp_async_mbarrier_arrive_noinc_shared(_barrier: *mut crate::barrier::Barrier) {
    unreachable!("cp_async_mbarrier_arrive_noinc_shared called outside CUDA kernel context")
}

/// Associates this thread's prior asynchronous copies with a shared-address barrier using balanced pending-count accounting.
/// Lowers to `cp.async.mbarrier.arrive.shared.b64 [<address>];`.
///
/// # Safety
/// `barrier` must point to a live, initialized, eight-byte-aligned mbarrier object in shared memory.
/// This thread must have issued the `cp.async` operations being associated with it.
/// The object must remain valid until those operations complete.
/// This form increments the pending count before scheduling the asynchronous arrival.
/// That increment must not exceed the barrier's pending-count limit.
#[inline(never)]
pub unsafe fn cp_async_mbarrier_arrive_shared(_barrier: *mut crate::barrier::Barrier) {
    unreachable!("cp_async_mbarrier_arrive_shared called outside CUDA kernel context")
}
