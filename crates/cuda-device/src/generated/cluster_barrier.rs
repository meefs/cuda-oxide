/*
 * SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
 * SPDX-License-Identifier: Apache-2.0
 */

// @generated by cuda-intrinsics-gen 0.1.0. DO NOT EDIT.
// Catalog schema/version: 44/0.1.0; intrinsic ABI: v1; catalog SHA-256: 1f72d053300ad70672b9cf851743dd05e0904f7222828eecd53d5ca88a4a0088.
// LLVM source: 1cb4e3833c1919c2e6fb579a23ac0e2b22587b7e.

// Included inside `cuda_device::cluster` to keep public paths stable.

/// Arrives at the cluster barrier with release ordering.
/// Lowers to `barrier.cluster.arrive;`.
///
/// # Safety
/// Each non-exited cluster thread must arrive exactly once before completion, then execute the matching wait.
#[inline(never)]
pub unsafe fn barrier_cluster_arrive() {
    unreachable!(
        "generated CUDA intrinsic `cuda_device::cluster::barrier_cluster_arrive` executed outside device compilation"
    )
}

/// Arrives at the cluster barrier in aligned mode with release ordering.
/// Lowers to `barrier.cluster.arrive.aligned;`.
///
/// # Safety
/// Each non-exited cluster thread must arrive exactly once before completion, then execute the matching wait.
/// Every non-exited thread in the warp must execute this aligned instruction with identical control flow.
#[inline(never)]
pub unsafe fn barrier_cluster_arrive_aligned() {
    unreachable!(
        "generated CUDA intrinsic `cuda_device::cluster::barrier_cluster_arrive_aligned` executed outside device compilation"
    )
}

/// Arrives at the cluster barrier without a release guarantee.
/// Lowers to `barrier.cluster.arrive.relaxed;`.
///
/// # Safety
/// Each non-exited cluster thread must arrive exactly once before completion, then execute the matching wait.
/// This relaxed arrival does not publish earlier memory accesses; use the required cluster-scope fence first.
#[inline(never)]
pub unsafe fn barrier_cluster_arrive_relaxed() {
    unreachable!(
        "generated CUDA intrinsic `cuda_device::cluster::barrier_cluster_arrive_relaxed` executed outside device compilation"
    )
}

/// Arrives at the cluster barrier in aligned mode without a release guarantee.
/// Lowers to `barrier.cluster.arrive.relaxed.aligned;`.
///
/// # Safety
/// Each non-exited cluster thread must arrive exactly once before completion, then execute the matching wait.
/// Every non-exited thread in the warp must execute this aligned instruction with identical control flow.
/// This relaxed arrival does not publish earlier memory accesses; use the required cluster-scope fence first.
#[inline(never)]
pub unsafe fn barrier_cluster_arrive_relaxed_aligned() {
    unreachable!(
        "generated CUDA intrinsic `cuda_device::cluster::barrier_cluster_arrive_relaxed_aligned` executed outside device compilation"
    )
}

/// Waits at the cluster barrier with acquire ordering.
/// Lowers to `barrier.cluster.wait;`.
///
/// # Safety
/// Each non-exited cluster thread must arrive exactly once before completion, then execute the matching wait.
#[inline(never)]
pub unsafe fn barrier_cluster_wait() {
    unreachable!(
        "generated CUDA intrinsic `cuda_device::cluster::barrier_cluster_wait` executed outside device compilation"
    )
}

/// Waits at the cluster barrier in aligned mode with acquire ordering.
/// Lowers to `barrier.cluster.wait.aligned;`.
///
/// # Safety
/// Each non-exited cluster thread must arrive exactly once before completion, then execute the matching wait.
/// Every non-exited thread in the warp must execute this aligned instruction with identical control flow.
#[inline(never)]
pub unsafe fn barrier_cluster_wait_aligned() {
    unreachable!(
        "generated CUDA intrinsic `cuda_device::cluster::barrier_cluster_wait_aligned` executed outside device compilation"
    )
}
